# Current Build State

Last aligned: 2026-06-24.

## Repo Verified

- Version `1024` cleans the no-unit job treatment in rendered Summary/Estimate documents. `Index.html` shows revision `149`. The shared Summary renderer now accepts explicit page-render options, and document/export render paths pass `allowEstimateViewRemoval:false` with `renderSurface:'document'`, so downloaded or emailed estimate PDFs keep the no-unit warning and inactive-job dimming but do not render the interactive `Remove Job X from estimate view` button. The interactive portal Summary/Estimate view still uses the default render options, keeps the remove button, and keeps the longer helper copy. Document/export no-unit warnings use the shorter one-line-friendly copy `Job X has no units entered. Job costs & turn times are excluded from header totals.` This is a renderer/UI-only change; pricing, header totals, turn-time exclusion, quantity logic, snapshot parsing, email recipient parsing, email backend routing, payment/tax/lifecycle/routing/token behavior, and Sheet schemas are unchanged. `clasp version` created `1024`, and the existing stable deployment ID was updated at `@1024 - Clean no-unit estimate document render`; no new deployment ID was created. Validation passed: `node --check apps-script/src/Code.js`, `node --check tools/validate-repo.mjs`, `npm run validate:runtime`, `VALIDATE_ALLOW_RUNTIME_CHANGES=1 npm run validate`, and `git diff --check`. Direct `/exec` returned HTTP `200` with `Development revision 149`, omitted stale revision `148`, referenced the stable deployment ID, and had zero targeted secret/private-link markers; public `/portal` returned HTTP `200`, referenced the stable deployment ID, retained route markers, and had zero targeted secret/private-link markers. No email-review suite was run, no estimate email was sent during this shell, no Sheet data was mutated, and no queue rows were cleared.
- Version `1023` refines the authenticated dashboard Project Peek header styling. `Index.html` shows revision `148`. The Project Peek title now renders with strong white heading color and a subtle text shadow for readability on the dark header. The header action area now gives the `Open Project` control a wider centered action region with substantially more separation from the red circular close button. The Project Peek `Open Project` hover/focus state now fills with the portal green/teal treatment and switches its text/icon to dark, while the close button remains the red/destructive control. This is a frontend CSS/layout-only change; dashboard data loading, project routing, token behavior, peek data parsing, lifecycle/payment/ACH/email logic, Sheet schemas, and backend contracts are unchanged. `clasp version` created `1023`, and the existing stable deployment ID was updated at `@1023 - Refine project peek header`; no new deployment ID was created. Validation passed: `node --check apps-script/src/Code.js`, `node --check tools/validate-repo.mjs`, `npm run validate:runtime`, `VALIDATE_ALLOW_RUNTIME_CHANGES=1 npm run validate`, and `git diff --check`. Direct `/exec` returned HTTP `200` with `Development revision 148`, omitted stale revision `147`, referenced the stable deployment ID, and had zero targeted secret/private-link markers; public `/portal` returned HTTP `200`, referenced the stable deployment ID, retained route markers, and had zero targeted secret/private-link markers. No email-review suite was run, no Sheet data was mutated, and no queue rows were cleared.
- Version `1022` refines no-unit job presentation in the Summary/Estimate document. `Index.html` shows revision `147`. Jobs that already render the no-units informational banner now keep the warning and remove button at full opacity, while the inactive job body, price-breaks block, and SKU/price rows render at `70%` opacity through the existing `isInformationalOnly` class. The remove button label now reads `Remove Job X from estimate view` with the dynamic job number, while preserving the existing remove/hide behavior. This is a UI/copy-only change; estimate pricing, header totals, turn-time exclusion, quantity logic, snapshot parsing, rendered artifacts, email/PDF generation, payment/tax/lifecycle/routing/token behavior, and Sheet schemas are unchanged. `clasp version` created `1022`, and the existing stable deployment ID was updated at `@1022 - Refine no-unit estimate jobs`; no new deployment ID was created. Validation passed: `node --check apps-script/src/Code.js`, `node --check tools/validate-repo.mjs`, `npm run validate:runtime`, `VALIDATE_ALLOW_RUNTIME_CHANGES=1 npm run validate`, and `git diff --check`. Direct `/exec` returned HTTP `200` with `Development revision 147`, omitted stale revision `146`, referenced the stable deployment ID, and had zero targeted secret/private-link markers; public `/portal` returned HTTP `200`, referenced the stable deployment ID, retained route markers, and had zero targeted secret/private-link markers. No email-review suite was run, no estimate email was sent, no Sheet data was mutated, and no queue rows were cleared.
- Version `1021` refines the Summary/Estimate tab share/download sidebar interaction. `Index.html` shows revision `146`. The `Share / Download Estimate` / `Share / Download Invoice` primary button now hides while the inline email entry panel is open, so the panel occupies that top control space; `Cancel` closes the panel and restores the at-rest button. Successful email sends now immediately close the panel, restore the button, clear the email input, and show a small `Email sent successfully` confirmation directly under the restored button for about three seconds using the at-rest primary button text color. Invalid-recipient and failed-send errors remain inside the entry panel, and the existing estimate PDF generation, recipient parsing, server email send path, credit card toggle, tax-exemption controls, pricing, snapshot data, payment logic, lifecycle state, routing, tokens, and Sheet schemas are unchanged. `clasp version` created `1021`, and the existing stable deployment ID was updated at `@1021 - Refine estimate share panel`; no new deployment ID was created. Validation passed: `node --check apps-script/src/Code.js`, `node --check tools/validate-repo.mjs`, `npm run validate:runtime`, `VALIDATE_ALLOW_RUNTIME_CHANGES=1 npm run validate`, and `git diff --check`. Direct `/exec` returned HTTP `200` with `Development revision 146`, omitted stale revision `145`, referenced the stable deployment ID, and had zero targeted secret/private-link markers; public `/portal` returned HTTP `200`, referenced the stable deployment ID, retained route markers, and had zero targeted secret/private-link markers. No email-review suite was run, no estimate email was sent, no Sheet data was mutated, and no queue rows were cleared.
- Version `1020` fixes authenticated dashboard project peek height for compact one/few-project dashboards and ships the pending dashboard account-card polish. `Index.html` shows revision `145`. The project peek backdrop now mounts to `document.body` and uses viewport-level fixed positioning instead of sizing inside the current dashboard card; the peek popover is constrained to the existing intended `760px` expanded height, capped by the viewport gutters, with the body remaining the internal scroll region. This keeps one-project dashboards compact before opening the peek while preventing the peek from collapsing to the short dashboard height. The ACH Payment Accounts card now aligns its value with the other account cards, shows `Manage accounts` by default, changes to `Set up an account` on hover/focus, and the generic green ACH helper bullet is removed when no ACH attention state exists. `clasp version` created `1020`, and the existing stable deployment ID was updated at `@1020 - Fix dashboard peek height`; no new deployment ID was created. Validation passed: `node --check apps-script/src/Code.js`, `node --check tools/validate-repo.mjs`, `npm run validate:runtime`, `VALIDATE_ALLOW_RUNTIME_CHANGES=1 npm run validate`, and `git diff --check`. Direct `/exec` returned HTTP `200` with `Development revision 145`, omitted stale revision `144`, referenced the stable deployment ID, and had zero targeted secret/private-link markers; public `/portal` returned HTTP `200`, referenced the stable deployment ID, retained route markers, and had zero targeted secret/private-link markers. No email-review suite was run, no Sheet data was mutated, no queue rows were cleared, and no lifecycle, status, payment, ACH, Stripe, email, routing, schema, or token logic was changed by this pass.
- Version `1019` hardens the Summary/Estimate PDF export path and ships the dashboard Open Project action-column alignment. `Index.html` shows revision `144`. Summary/Estimate download/share now builds the PDF from a fresh offscreen sanitized export host instead of mutating and capturing the live `summaryRoot`; the export host reuses the canonical Summary data, `cloneSummaryDataForExport_()`, `populateSummaryPages_()`, pagination stabilization, image proxying, image wait/replacement, the existing `html-to-image` renderer, placeholder retry, and optional `html2canvas` fallback. The export now performs one full host rebuild retry after render/package failure, records redacted stage diagnostics for `library_load`, `page_collect`, `image_proxy`, `image_wait`, `canvas_render`, `pdf_package`, and `server_send`, and exposes a non-sending debug hook `RED_THREADS_BUILD_SUMMARY_EXPORT_PDF()` for browser smoke. Summary share UI now distinguishes browser PDF-generation failures from server email-send failures without changing recipient parsing, server `sendSummaryEstimatePdfEmail()`, document references, portal-rendered attachment policy, lifecycle/payment logic, routing, Sheet schemas, or email routing. The dashboard projects table now fills the available dashboard container while preserving fixed thumbnail, Project Details, and Order Progress column widths; only the Open Project action column absorbs the remaining width, with buttons centered in that expanded right-side column. `clasp version` created `1019`, and the existing stable deployment ID was updated at `@1019 - Harden estimate PDF export`; no new deployment ID was created. Validation passed: `node --check apps-script/src/Code.js`, `node --check tools/validate-repo.mjs`, `npm run validate:runtime`, `VALIDATE_ALLOW_RUNTIME_CHANGES=1 npm run validate`, `git diff --check`, and an additional main browser-script parse check. Direct `/exec` returned HTTP `200` with `Development revision 144`, omitted stale revision `143`, referenced the stable deployment ID, and had zero targeted secret/private-link markers; public `/portal` returned HTTP `200`, referenced the stable deployment ID, retained route markers, and had zero targeted secret/private-link markers. No email-review suite was run, no estimate email was sent, and no active tabs, fixture tabs, queue rows, Script Properties, Stripe config, lifecycle/email logic, payment rules, routing rules, or Sheet schemas were mutated by this pass.
- Version `1018` optimizes authenticated dashboard loading after the metadata-first dashboard pass. `Index.html` shows revision `143`. Dashboard summary rows now include lightweight thumbnail candidates derived from the first visible/selected print job, preferring art mockups before preview/garment fallbacks, while keeping full peek payloads out of the first-paint response. Dashboard project cache keys moved to `v15` so stale summary rows without thumbnails are not reused. `getDashboardProjectStatusBatch` now resolves the account/session and export row map once per batch, preloads latest order rows by token, and passes the shared account/order context into the canonical projection builder. The frontend now hydrates progress in viewport-priority batches: the first visible batch defaults to `8` rows, then background batches continue in chunks of `4`; peek prefetch uses a priority queue that promotes visible, hovered, focused, or opened projects and continues slowly in the background. Redacted timing marks were added for dashboard status and peek paths without logging raw tokens, emails, URLs, snapshot JSON, or payload bodies. `clasp version` created `1018`, and the existing stable deployment ID was updated at `@1018 - Optimize dashboard loading`; no new deployment ID was created. Validation passed: `node --check apps-script/src/Code.js`, `node --check tools/validate-repo.mjs`, `npm run validate:runtime`, `VALIDATE_ALLOW_RUNTIME_CHANGES=1 npm run validate`, and `git diff --check`. Direct `/exec` returned HTTP `200` with `Development revision 143`, omitted stale revision `142`, referenced the stable deployment ID, and had zero targeted secret/private-link markers; public `/portal` returned HTTP `200`, referenced the stable deployment ID, retained route markers, and had zero targeted secret/private-link markers. Read-only runtime status remained `activeTabState: live_like` with `EXPORT_LOG` `20` active rows, `PORTAL_ORDERS` `7`, `PORTAL_STRIPE_EVENTS` `7`, `PORTAL_EMAIL_QUEUE` unchanged at two `sent` rows, and scheduler trigger count `1`. Browser smoke on the authenticated dashboard showed `18` unique dashboard rows, `18` image-backed thumbnails, progress hydration moving from visible-first partial state to `18 / 18` hydrated rows with `0` skeletons, and project peek open/close still functional with no captured console warnings/errors. No active tabs, fixture tabs, queue rows, Script Properties, Stripe config, lifecycle/email logic, payment rules, routing rules, or Sheet schemas were mutated by this pass.
- Version `1017` fixes the authenticated dashboard row and project peek layout regressions found after the dashboard login/load hardening pass. `Index.html` shows revision `142`. The dashboard projects table remains fixed-layout, but the Project Details column is reduced from `430px` to `301px` and the table width from `1080px` to `951px`, so long project names wrap inside the details column instead of pushing the progress and Open Project columns offscreen. Project peek headers now reserve explicit title/action/close columns, cap the title area at `380px`, clamp long titles to two lines, and keep the Open Project button and close control clickable. Peek dismissal now uses a capture-phase document `pointerdown` outside-click handler, so clicking anywhere outside the peek closes it while preserving the close button and Escape behavior. `clasp version` created `1017`, and the existing stable deployment ID was updated at `@1017 - Fix dashboard peek layout`; no new deployment ID was created. Validation passed: `node --check apps-script/src/Code.js`, `node --check tools/validate-repo.mjs`, `npm run validate:runtime`, `VALIDATE_ALLOW_RUNTIME_CHANGES=1 npm run validate`, and `git diff --check`. Direct `/exec` returned HTTP `200` with `Development revision 142` and the stable deployment ID; public `/portal` returned HTTP `200` and referenced the stable deployment ID. Read-only runtime status remained `activeTabState: live_like` with `EXPORT_LOG` `20` active rows, `PORTAL_ORDERS` `7`, `PORTAL_STRIPE_EVENTS` `7`, `PORTAL_EMAIL_QUEUE` unchanged at two `sent` rows, and scheduler trigger count `1`. No active tabs, fixture tabs, queue rows, Script Properties, Stripe config, lifecycle/email logic, routing rules, or Sheet schemas were mutated by this pass.
- Version `1016` hardens dashboard login/load performance after live runtime restoration. `loginUser` now accepts `skipDashboardHomeData:true`, allowing authentication/session creation to return without building dashboard projects; the frontend login path uses that flag so the loader can move from `Logging you in` to `Loading your dashboard` before fetching dashboard rows. Dashboard home requests now use a `summary` profile for first paint: the server returns metadata-only rows, skips embedded header/blank-token rows, deduplicates by token, keeps the newest export row by exported/created timestamp with row number tie-break, avoids snapshot/order-draft/peek parsing, and defers status/progress to `getDashboardProjectStatusBatch`. The dashboard cache key includes the response profile, and preview/peek prefetch waits until after first paint/status hydration and is capped to the first three queued peeks. `Index.html` shows revision `141`. `clasp version` created `1016`, and the existing stable deployment ID was updated at `@1016 - Harden dashboard login load`; no new deployment ID was created. Validation passed: `node --check apps-script/src/Code.js`, `node --check tools/validate-repo.mjs`, `npm run validate:runtime`, `VALIDATE_ALLOW_RUNTIME_CHANGES=1 npm run validate`, and `git diff --check`. Direct `/exec` returned HTTP `200` with `Development revision 141` and the stable deployment ID; public `/portal` returned HTTP `200` and referenced the stable deployment ID. Read-only runtime status remained `activeTabState: live_like` with `EXPORT_LOG` `20` active rows, `PORTAL_ORDERS` `7`, `PORTAL_STRIPE_EVENTS` `7`, `PORTAL_EMAIL_QUEUE` still at `2` `sent` rows, and scheduler trigger count `1` at 9 AM America/Detroit. No active tabs, fixture tabs, queue rows, Script Properties, Stripe config, or Sheet schemas were mutated by this pass.
- Version `1015` completes Phase 3 live runtime restoration. A protected headless restore helper was added because the existing repo had read-only status tooling but no safe server-side active-tab restore path, and the local REST path would have required enabling the Google Sheets API on the clasp OAuth project. `Index.html` shows revision `140`. `clasp version` created `1015`, and the existing stable deployment ID was updated at `@1015 - Add live runtime restore helper`; no new deployment ID was created. The restore used the private pre-fixture workbook titled `CALC_EST_OST_EXPORT_LOG active reset backup 2026-06-19 20-10` with Drive IDs redacted, created a private rollback copy of the fixture-loaded active workbook, then mutated only `EXPORT_LOG`, `PORTAL_ORDERS`, and `PORTAL_STRIPE_EVENTS` below their preserved header rows. `FIXTURE_*` tabs, `PORTAL_EMAIL_QUEUE`, `USERS`, `USER_SESSIONS`, `PORTAL_ACCOUNTS`, Script Properties, Stripe config, Apps Script config, and deployment settings were not touched. Post-restore `npm run email-review:status` returned `activeTabState: live_like`: `EXPORT_LOG` has `917` active data rows, `PORTAL_ORDERS` has `7`, and `PORTAL_STRIPE_EVENTS` has `7`, all with fixture match mode `none`; `PORTAL_EMAIL_QUEUE` still has `2` `sent` rows; the scheduler handler `processPurchaseOrderPaymentReminderSchedule` remains installed at hour `9` America/Detroit with trigger count `1`. Bounded sentinel searches found `0` active `Fixture`/`EmailReview` matches across the restored tabs. No-mutation fixture validation remained clean: `npm run email-review:suite -- --dry-run --fixture-source=storage` returned `ok:true`, sent `0`, skipped `1`, failed `0`, attachment fallback `44`, contradiction warnings/errors `0`, parity failures `0`, `activeTabsMutated:false`, and `queueCleared:false`; matrix validation returned `ok:true` with `37 / 37` required cases covered and `0` intent mismatches. Direct `/exec` returned HTTP `200` with `Development revision 140` and the stable deployment ID; public `/portal` returned HTTP `200` and referenced the stable deployment ID. No live review-suite emails were sent.
- Version `1014` completes Phase 1 email hardening/no-mutation review infrastructure. Standard ACH verification team copy now says verification follow-up occurs after `4 days` instead of `4 business days`, matching the existing 4-calendar-day scheduler. The headless review suite now supports `--fixture-source=storage`, which reads `FIXTURE_EXPORT`, `FIXTURE_PORTAL_ORDERS`, and `FIXTURE_STRIPE_EVENTS` directly without copying fixture rows into active runtime tabs or clearing `PORTAL_EMAIL_QUEUE`; the existing default review-suite reset path is unchanged. A protected read-only `email-review:status` command reports active-tab fixture/live classification, fixture header compatibility, redacted queue counts by status, and the daily scheduler trigger state without exposing tokens, emails, URLs, row JSON, or secrets. Dry-run review results now include client/team parity assertions for paired order-context emails across normalized progress labels/statuses, shared timing fields, and history rows. `Index.html` shows revision `139`. `clasp version` created `1014`, and the existing stable deployment ID was updated at `@1014 - Harden email review fixture mode`; transient version `1013` was superseded before commit after the status classifier was tightened to treat fixture-identity-matched `PORTAL_ORDERS` generated-cell drift as fixture-loaded. Direct Apps Script `/exec` returned HTTP `200` with `Development revision 139`, referenced the stable deployment ID, omitted stale revision `138`, and had zero targeted app secret/private-link markers; public `/portal` returned HTTP `200` and referenced the stable deployment ID. The read-only status command returned `activeTabState: fixture_loaded`: `EXPORT_LOG` and `PORTAL_STRIPE_EVENTS` matched fixture storage exactly, `PORTAL_ORDERS` matched fixture row identity, `PORTAL_EMAIL_QUEUE` had `2` sent rows, and the scheduler handler `processPurchaseOrderPaymentReminderSchedule` remained installed at hour `9` America/Detroit with trigger count `1`. Default protected dry run returned `ok:true`, total `91`, sent `0`, skipped `1`, failed `0`, attachment fallback `44`, contradiction warnings/errors `0`, parity failures `0`, active-tab mutation `false`, queue clear `false`, and matrix `ok:true` with `37` required, `37` covered, `0` skipped/omitted, `0` missing, and `0` intent mismatches. Storage-source dry run returned the same clean counts with `fixtureSource: storage`, active-tab mutation `false`, and queue clear `false`.
- Version `1012` refines portal-generated email subject lines through centralized production subject helpers while preserving lifecycle communication rules, recipient routing, queue job types, idempotency, attachment policy, portal links, Team Mode authorization, payment/production/PO logic, Sheet schemas, Script Properties, Stripe config, and the existing stable deployment ID. Production subjects now use short human-readable action/status phrases with suffixes such as ` — Project #...`, team subjects use `[Team]`, and fixture/document labels such as `Invoice EmailReview-v1`, `Receipt for Invoice EmailReview-v1`, and `Fixture - Production Complete` are no longer emitted by runtime subject builders. The review-suite omission maps are intentionally cleared again, so all configured review emails are visible unless a true fixture/artifact safety skip applies; the local `Standard ACH verification 4-day reminder client` action-card CTA suppression is preserved. `docs/EMAIL_SUBJECT_AUDIT.md` records the old/new subject audit. `Index.html` shows revision `138`. `clasp version` created `1012`, and the existing stable deployment ID was updated at `@1012 - Refine portal email subjects`; transient version `1011` was superseded before commit after the post-deploy dry run exposed utility subjects that needed project suffix context. Direct Apps Script `/exec` returned HTTP `200` with `Development revision 138`, referenced the stable deployment ID, omitted stale revision `137`, and had zero targeted sensitive markers; public `/portal` returned HTTP `200`, referenced the stable deployment ID, and had zero targeted sensitive markers. Protected post-deploy dry run returned `ok:true`, total `76`, sent `0`, skipped `1`, failed `0`, attachment fallback `44`, contradiction warnings/errors `0`, and matrix validation `ok:true` with `37` required, `37` covered, `0` skipped/omitted, `0` missing, and `0` intent mismatches. The owner-approved live suite returned `ok:true`, sent `62` actual review emails (`34` client, `24` team, `4` AP), skipped `1`, failed `0`, attachment fallback `44`, and contradiction warnings/errors `0`. The only skipped label was `Submitted tax-form copy client` because the submitted artifact is unavailable in fixtures. The live non-dry-run path reset `FIXTURE_EXPORT -> EXPORT_LOG`, `FIXTURE_PORTAL_ORDERS -> PORTAL_ORDERS`, and `FIXTURE_STRIPE_EVENTS -> PORTAL_STRIPE_EVENTS`, then cleared `PORTAL_EMAIL_QUEUE`; fixture-storage tabs were not mutated. The live send artifact is an inbox-send receipt and does not include dry-run-only assertion rows, so the protected dry-run JSON remains the authoritative matrix artifact.
- Version `1010` adds scheduled Standard ACH bank-verification 4-calendar-day reminder emails through the existing lifecycle email shell and queue. The existing daily handler `processPurchaseOrderPaymentReminderSchedule` now also runs the ACH verification reminder scheduler, preserving trigger compatibility; protected trigger status reports handler `processPurchaseOrderPaymentReminderSchedule`, timezone `America/Detroit`, hour `9`, trigger count `1`, installed `true`. New queue job types `ach_bank_verification_reminder_email` and `ach_bank_verification_reminder_team_alert_email` send one client reminder and one team alert when a standard checkout ACH order remains in bank-verification/microdeposit-required state four calendar days after the verification anchor. The client email uses `payment_verification_required` intent, blue `Action required` styling, a neutral `View Project #... in the portal` CTA, and Stripe microdeposit-verification guidance; the team email uses green `Potential action` copy, says the client was notified, and suggests outreach without rendering an action-card Team Mode button. `Index.html` shows revision `137`. `clasp version` created `1010`, and the existing stable deployment ID was updated at `@1010 - Add ACH verification reminder emails`; transient version `1009` was superseded before Git commit after the live suite exposed CTA metadata that needed to report `view_project` rather than `pay`. Direct Apps Script `/exec` returned HTTP `200` with `Development revision 137`, omitted stale revision `136`, referenced the stable deployment ID, retained route-bridge markers, and had zero targeted sensitive markers; public `/portal` returned HTTP `200`, referenced the stable deployment ID, retained route-bridge markers, and had zero targeted sensitive markers. Per owner instruction, no protected email dry run was run. The final live suite on version `1010` returned `ok:true`, sent `2` actual emails, skipped `61`, failed `0`, attachment fallback `2`, and contradiction warnings/errors `0`; matrix validation against that live output returned `ok:true` with `37` required cases, `1` covered, `36` skipped/omitted, `0` missing, and `0` intent mismatches. Actual sent labels were `Standard ACH verification 4-day reminder client` and `Standard ACH verification 4-day reminder team`. The live non-dry-run path reset `FIXTURE_EXPORT -> EXPORT_LOG`, `FIXTURE_PORTAL_ORDERS -> PORTAL_ORDERS`, and `FIXTURE_STRIPE_EVENTS -> PORTAL_STRIPE_EVENTS`, then cleared `PORTAL_EMAIL_QUEUE`; fixture-storage tabs were not mutated.
- Version `1008` hides the reviewed payment team/completed PO receipt fixtures and finishes the requested payment-failure copy cleanup. The review-suite omission map now hides `PO payment received production complete client`, `PO submitted team`, `Standard ACH failed team`, `Card failed team`, `AP ACH receipt team`, `PO payment received production complete team`, and `AP ACH failed team`. PO submitted team copy now preserves `The invoice/receipt for the client's order is attached to this email.` even when the review artifact is a receipt. PO payment-received completed-production team copy removes the extra `Payment was marked received in Team Mode.` sentence; the system still does not claim a card/ACH/check remittance method for PO receipts because Team Mode currently records only receipt timestamp/actor, not an outside payment form. Standard ACH, Card, and AP ACH payment-failure client/team headings now include `production cannot proceed` where those failed-payment emails render, and AP ACH failed team copy now says the client was notified, says success notifications go to the client, Accounts Payable, and Red Threads, and uses the revised potential-action contact line. `Index.html` shows revision `135`. `clasp version` created `1008`, and the existing stable deployment ID was updated at `@1008 - Hide reviewed payment team emails`. Direct Apps Script `/exec` returned HTTP `200` with `Development revision 135`, omitted stale revision `134`, referenced the stable deployment ID, and had zero targeted sensitive markers; public `/portal` returned HTTP `200`, referenced the stable deployment ID, retained route-bridge markers, and had zero targeted sensitive markers. Per owner instruction, no email-review dry run was run. The owner-approved live suite returned `ok:true`, sent `0` actual emails, skipped `61`, failed `0`, attachment fallback `0`, and contradiction warnings/errors `0`; it also returned one non-email `PO paid suppression assertion` row. The live non-dry-run path reset `FIXTURE_EXPORT -> EXPORT_LOG`, `FIXTURE_PORTAL_ORDERS -> PORTAL_ORDERS`, and `FIXTURE_STRIPE_EVENTS -> PORTAL_STRIPE_EVENTS`, then cleared `PORTAL_EMAIL_QUEUE`; fixture-storage tabs were not mutated.
- Version `1007` adds the missing client-facing completed-production PO receipt review fixture and refines the remaining visible team payment-review emails. The review suite now sends `PO payment received production complete client` as a separate client fixture while keeping the generic `PO submitted client` and `PO payment received client` hidden. `Standard ACH failed team` and `AP ACH failed team` now follow the `Card failed team` potential-action structure while preserving ACH/AP pathway language, `AP ACH pending team` copy now removes the redundant Red Threads-order wording and says client/team will be notified when payment clears, and both `AP ACH pending team` and generic `PO payment received team` are hidden with `owner_reviewed_hidden`. PO payment-received history now renders the Purchase Order number and payment-received date as separate rows, and completed PO receipt team copy truthfully says the payment was marked received in Team Mode because no separate remittance method is stored. `Index.html` shows revision `134`. `clasp version` created `1007`, and the existing stable deployment ID was updated at `@1007 - Add completed PO client review receipt`. Direct Apps Script `/exec` returned HTTP `200` with `Development revision 134`, omitted stale revision `133`, referenced the stable deployment ID, and had zero targeted sensitive markers; public `/portal` returned HTTP `200`, referenced the stable deployment ID, retained route-bridge markers, and had zero targeted sensitive markers. Per owner instruction, no email-review dry run was run. The owner-approved live suite returned `ok:true`, sent `7` actual emails, skipped `54`, failed `0`, attachment fallback `7`, and contradiction warnings/errors `0` across sent results; it also returned one non-email paid-PO suppression assertion row. Actual sent labels were `Standard ACH failed team`, `AP ACH receipt team`, `AP ACH failed team`, `Card failed team`, `PO submitted team`, `PO payment received production complete client`, and `PO payment received production complete team`. The live non-dry-run path reset `FIXTURE_EXPORT -> EXPORT_LOG`, `FIXTURE_PORTAL_ORDERS -> PORTAL_ORDERS`, and `FIXTURE_STRIPE_EVENTS -> PORTAL_STRIPE_EVENTS`, then cleared `PORTAL_EMAIL_QUEUE`; fixture-storage tabs were not mutated.
- Version `1006` refines completed-production receipt communications. Payment-received email copy for Standard ACH receipt, AP ACH receipt, card paid, manual payment received, and PO payment received now detects when lifecycle production is already complete and suppresses stale production-started/continuing/action language. The completed PO payment receipt team review fixture renders as `PO payment received production complete team`, keeps receipt intent metadata (`po_payment_received`) while showing `productionDisposition: complete`, and reports no contradiction warnings/errors. `PO submitted client` is now hidden with `owner_reviewed_hidden`; `PO payment received client` remains hidden. `Index.html` shows revision `133`. `clasp version` created `1006`, and the existing stable deployment ID was updated at `@1006 - Refine completed PO receipt emails`; transient version `1005` was superseded before Git commit after the live suite showed the completed receipt branch should keep receipt intent metadata. Direct Apps Script `/exec` returned HTTP `200` with `Development revision 133`, omitted stale revision `132`, referenced the stable deployment ID, and had zero targeted sensitive markers; public `/portal` returned HTTP `200`, referenced the stable deployment ID, retained route-bridge markers, and had zero targeted sensitive markers. Per owner instruction, no email-review dry run was run. The owner-approved live suite returned `ok:true`, sent `8`, skipped `52`, failed `0`, attachment fallback `8`, and contradiction warnings/errors `0` across sent results. Final sent labels were `Standard ACH failed team`, `AP ACH pending team`, `AP ACH receipt team`, `AP ACH failed team`, `Card failed team`, `PO submitted team`, `PO payment received team`, and `PO payment received production complete team`. The live non-dry-run path reset `FIXTURE_EXPORT -> EXPORT_LOG`, `FIXTURE_PORTAL_ORDERS -> PORTAL_ORDERS`, and `FIXTURE_STRIPE_EVENTS -> PORTAL_STRIPE_EVENTS`, then cleared `PORTAL_EMAIL_QUEUE`; fixture-storage tabs were not mutated.
- Version `1004` refines Purchase Order review email visibility and PO-number-specific copy across the PO pathway. `PO submitted client` is now visible in the owner review-suite again, while `PO payment received client` is hidden with `owner_reviewed_hidden`. PO submitted/payment-received/reminder/past-due/late-fee client and team copy now uses the client-supplied Purchase Order number when present, including `Payment has been received for Purchase Order #...` for the PO payment-received client path and `PO #... payment received` subject language for receipt paths. The prior local `Card failed team` copy refinement is included in this ship. `Index.html` shows revision `131`. `clasp version` created `1004`, and the existing stable deployment ID was updated at `@1004 - Refine PO review email copy`; a transient version `1003` was superseded before Git commit after the live suite exposed a redundant PO receipt subject. Direct Apps Script `/exec` returned HTTP `200` with `Development revision 131`, omitted stale revision `130`, referenced the stable deployment ID, and had zero targeted sensitive markers; public `/portal` returned HTTP `200`, referenced the stable deployment ID, and had zero targeted sensitive markers. Per owner instruction, no email-review dry run was run. The owner-approved live suite returned `ok:true`, sent `8`, skipped `51`, failed `0`, attachment fallback `8`, and contradiction warnings/errors `0` across sent results. Final sent labels were `Standard ACH failed team`, `AP ACH pending team`, `AP ACH receipt team`, `AP ACH failed team`, `Card failed team`, `PO submitted client`, `PO submitted team`, and `PO payment received team`. The live non-dry-run path reset `FIXTURE_EXPORT -> EXPORT_LOG`, `FIXTURE_PORTAL_ORDERS -> PORTAL_ORDERS`, and `FIXTURE_STRIPE_EVENTS -> PORTAL_STRIPE_EVENTS`, then cleared `PORTAL_EMAIL_QUEUE`; fixture-storage tabs were not mutated.
- Version `1002` refines PO/ACH team review copy and PO open-terms progress labels. Standard ACH verification team copy now says the client paid via ACH and is linking their bank for the first time, explains Stripe microdeposit verification, and says the client/team will be notified if verification is not completed within four business days. AP ACH receipt team and PO payment received team now use the same operational payment-recorded/client-notified/proceed-through-production structure as the other receipt team emails while preserving AP/PO payment language; PO payment received team states the payment was recorded through Team Mode because no separate outside-remittance method is captured. Manual payment pending team now states that the portal is locked and can be unlocked in Team Mode. PO submitted team now renders `Purchase Order received`, includes the PO number when present, states payment due date from account terms, and uses production-first next action copy. PO/open-terms payment progress bars now show `Pay by M/D` when a due date is available instead of `Current action`. The review-suite omission map now hides `Standard ACH pending team`, `Standard ACH verification team`, `Standard ACH receipt team`, `Card paid team`, `Manual payment pending team`, `Manual payment received team`, and `PO payment 60-day escalation team`; `PO payment received client` is unhidden. `Index.html` shows revision `129`. `clasp version` created `1002`, and the existing stable deployment ID was updated at `@1002 - Refine PO and ACH team emails`. Direct Apps Script `/exec` returned HTTP `200` with `Development revision 129`, omitted stale revision `128`, referenced the stable deployment ID, and had zero targeted sensitive markers; public `/portal` returned HTTP `200`, referenced the stable deployment ID, and had zero targeted sensitive markers. Per owner instruction, no email-review dry run was run. The owner-approved live suite returned `ok:true`, sent `8`, skipped `51`, failed `0`, attachment fallback `8`, and contradiction warnings/errors `0` across sent results. The live non-dry-run path reset `FIXTURE_EXPORT -> EXPORT_LOG`, `FIXTURE_PORTAL_ORDERS -> PORTAL_ORDERS`, and `FIXTURE_STRIPE_EVENTS -> PORTAL_STRIPE_EVENTS`, then cleared `PORTAL_EMAIL_QUEUE`; fixture-storage tabs were not mutated.
- Version `1001` fixes the team potential-action title resolver and hides the owner-reviewed production-complete team examples from live review-suite sends. `optional_initiate_production` is now normalized before the team action-card status title is resolved, so Standard ACH/AP ACH pending team emails render `Potential action` instead of falling through to `No action required`; the matching body label now says `Potential action:` instead of `Optional action:`. Team action-card color logic continues to render `Action required` blue, potential action titles green, and `No action required` red; client/AP behavior is unchanged. `Production complete shipping team` and `Production complete pickup team` are now hidden with `owner_reviewed_hidden`. `Index.html` shows revision `128`. `clasp version` created `1001`, and the existing stable deployment ID was updated at `@1001 - Refine team potential action emails`. Direct Apps Script `/exec` returned HTTP `200` with `Development revision 128`, omitted stale revision `127`, referenced the stable deployment ID, and had zero targeted sensitive markers; public `/portal` returned HTTP `200`, referenced the stable deployment ID, and had zero targeted sensitive markers. Protected dry-run/matrix validation returned `ok:true`, total `72`, skipped `45`, failed `0`, attachment fallback `14`, contradiction warnings/errors `0`, matrix `ok:true` with `36` required, `16` covered, `20` skipped/omitted, `0` missing, and `0` intent mismatches. The owner-approved live suite returned `ok:true`, sent `14` team review emails, skipped `45`, failed `0`, attachment fallback `14`, and contradiction warnings/errors `0`. The live non-dry-run path reset `FIXTURE_EXPORT -> EXPORT_LOG`, `FIXTURE_PORTAL_ORDERS -> PORTAL_ORDERS`, and `FIXTURE_STRIPE_EVENTS -> PORTAL_STRIPE_EVENTS`, then cleared `PORTAL_EMAIL_QUEUE`; fixture-storage tabs were not mutated.
- Version `1000` refines team payment action email copy and team action-card title colors. `Manual payment pending team` now renders as `Physical payment pending`, says the portal order was placed with physical payment, names the client in the invoice/receipt attachment sentence, states that payment must be received before production can begin, and uses the updated Team Mode next action for marking manual payment received. `Standard ACH receipt team`, `Card paid team`, and `Manual payment received team` now share the same operational `payment recorded/client notified/proceed through production` structure. Team action-card title color logic now renders `Action required` blue, potential/optional action titles green, and `No action required` red; client/AP action-card behavior is unchanged. Team-only rush awareness from `amountRush > 0` is included in the shipped runtime: team order-context emails can show a red `Rush order` header marker, a red rush production timing line when active production has a target, and a `Rush charge` Project Details row. `Index.html` shows revision `127`. `clasp version` created `1000`, and the existing stable deployment ID was updated at `@1000 - Refine team payment action emails`. Direct Apps Script `/exec` returned HTTP `200` with `Development revision 127`, omitted stale revision `126`, referenced the stable deployment ID, and had zero targeted sensitive markers; public `/portal` returned HTTP `200`, referenced the stable deployment ID, and had zero targeted sensitive markers. Protected dry-run/matrix validation returned `ok:true`, total `72`, skipped `43`, failed `0`, attachment fallback `14`, contradiction warnings/errors `0`, matrix `ok:true` with `36` required, `17` covered, `19` skipped/omitted, `0` missing, and `0` intent mismatches. The owner-approved live suite returned `ok:true`, sent `16` team review emails, skipped `43`, failed `0`, attachment fallback `14`, and contradiction warnings/errors `0`. The live non-dry-run path reset `FIXTURE_EXPORT -> EXPORT_LOG`, `FIXTURE_PORTAL_ORDERS -> PORTAL_ORDERS`, and `FIXTURE_STRIPE_EVENTS -> PORTAL_STRIPE_EVENTS`, then cleared `PORTAL_EMAIL_QUEUE`; fixture-storage tabs were not mutated.
- Version `999` refines the remaining production-complete review email copy and PO reminder header/body clarity. `Production complete pickup client` now uses a blue `Action required` action-card title, removes the action-card CTA, removes the redundant completed-jobs/payment status sentence, and uses the updated pickup copy with `505 South Saginaw Rd, Midland, Michigan 48640` plus Monday-Friday `9am - 5pm` pickup hours. `Production complete shipping PO unpaid client` is now hidden from owner review-suite sends and suppresses the action-card CTA if rendered. PO payment reminder/past-due/late-fee client headings no longer duplicate the project number, and next-action copy no longer repeats the full due-date/terms phrase. `Index.html` shows revision `126`. `clasp version` created `999`, and the existing stable deployment ID was updated at `@999 - Refine production complete review emails`. Direct Apps Script `/exec` returned HTTP `200` with `Development revision 126`, omitted stale revision `125`, and referenced the stable deployment ID; public `/portal` returned HTTP `200` and referenced the stable deployment ID. Targeted secret/private-link marker checks found zero hits. Protected dry run returned `ok:true`, total `72`, sent `0`, skipped `35`, failed `0`, attachment fallback `22`, contradiction warnings/errors `0`; matrix validation returned `ok:true` with `36` required, `23` covered, `13` skipped/omitted, `0` missing, and `0` intent mismatches. The owner-approved live suite returned `ok:true`, sent `24` review emails (`8` client, `16` team, `0` AP), skipped `35`, failed `0`, attachment fallback `22`, and contradiction warnings/errors `0`. The live non-dry-run path reset `FIXTURE_EXPORT -> EXPORT_LOG`, `FIXTURE_PORTAL_ORDERS -> PORTAL_ORDERS`, and `FIXTURE_STRIPE_EVENTS -> PORTAL_STRIPE_EVENTS`, then cleared `PORTAL_EMAIL_QUEUE`; fixture-storage tabs were not mutated.
- Version `998` refines client email action-card display and PO reminder timing copy. Client-recipient `Action required` action-card titles now render blue even when an older client copy model passes a red title tone, while team `Action required` remains red. Completed-production email timing now prefers `All job production completed: {date}` and suppresses stale `Estimated production completion` lines after production is complete. PO payment reminder/past-due/late-fee copy now says payment is due or was due under the `{terms}` terms on the account. `Index.html` shows revision `125`. The temporary Apps Script authorization bridge remains removed; `appsscript.json` is unchanged in final source. `clasp version` created `998`, and the existing stable deployment ID was updated at `@998 - Refine client email action status`. Direct Apps Script `/exec` returned HTTP `200` with `Development revision 125`, omitted stale revision `124`, and referenced the stable deployment ID; public `/portal` returned HTTP `200` and referenced the stable deployment ID. Targeted secret/private-link marker checks found zero hits. Protected dry run returned `ok:true`, total `72`, sent `0`, skipped `34`, failed `0`, attachment fallback `23`, contradiction warnings/errors `0`; matrix validation returned `ok:true` with `36` required, `23` covered, `13` skipped/omitted, `0` missing, and `0` intent mismatches. The owner-approved live suite returned `ok:true`, sent `25`, skipped `34`, failed `0`, attachment fallback `23`, and contradiction warnings/errors `0`. The live non-dry-run path reset `FIXTURE_EXPORT -> EXPORT_LOG`, `FIXTURE_PORTAL_ORDERS -> PORTAL_ORDERS`, and `FIXTURE_STRIPE_EVENTS -> PORTAL_STRIPE_EVENTS`, then cleared `PORTAL_EMAIL_QUEUE`; fixture-storage tabs were not mutated.
- Version `997` installs the PO payment reminder scheduler after explicit owner approval to run it while active tabs remain fixture-loaded/test-recipient oriented. `Code.js` now exposes protected headless install/status actions for the scheduler, guarded by the existing email-review trigger secret, and `Index.html` shows revision `124`. A temporary authorization bridge was deployed at versions `995`/`996` to grant Apps Script trigger permissions, then removed from the final repo/stable source so `appsscript.json` remains unchanged. The final stable deployment ID was updated at `@997 - Install PO reminder scheduler`. Direct Apps Script `/exec` returned HTTP `200` with `Development revision 124`, public `/portal` returned HTTP `200`, the stable deployment ID remained present, and targeted secret/private-link checks found zero actual sensitive markers. The installed time-driven trigger status is `ok:true`, handler `processPurchaseOrderPaymentReminderSchedule`, timezone `America/Detroit`, hour `9`, trigger count `1`, installed `true`. Active runtime tabs remain fixture-loaded after the version `993` live-suite reset, and the owner explicitly accepted fixture-mode scheduler activation.
- Version `993` adds purchase-order-only scheduled payment reminder and late-fee email support through the existing lifecycle email shell and `PORTAL_EMAIL_QUEUE`. The new scheduler scans active submitted PO orders with unpaid payment state, enqueues only currently due milestones, and renders client reminder/past-due/late-fee notices plus a 60-day team escalation with lifecycle-derived progress, Project Details, Production Time, history, footer, communication metadata, and optional invoice/status attachments. New scheduled milestones cover 5-business-day and 1-business-day pre-due reminders, first-business-day past-due notice, 5-calendar-day 2.5% late-fee notice, 30-calendar-day 7.5% late-fee notice, 60-calendar-day 12.5% late-fee notice, and 60-day team escalation; matrix coverage now requires `36` cases. `Card paid client` is also hidden from owner review-suite sends with the existing `owner_reviewed_hidden` policy, and the prior local production-complete shipping client copy refinement is included. `Index.html` shows revision `121`. `clasp version` created `993`, the existing stable deployment ID was updated at `@993 - Add PO payment reminder emails`, direct Apps Script `/exec` returned HTTP `200` with `Development revision 121`, public `/portal` returned HTTP `200`, the stable deployment ID remained present, and targeted secret/private-link checks found zero actual sensitive markers. Protected post-deploy dry run returned `ok:true`, total `72`, sent `0`, skipped `34`, failed `0`, attachment fallback `23`, contradiction warnings/errors `0`, and matrix `ok:true` with `36` required, `23` covered, `13` skipped/omitted, `0` missing, and `0` intent mismatches. A subsequent owner-approved live suite returned `ok:true`, sent `25` review emails (`9` client, `16` team, `0` AP), skipped `34`, failed `0`, attachment fallback `23`, and contradiction warnings/errors `0`; the live non-dry-run path reset `FIXTURE_EXPORT -> EXPORT_LOG`, `FIXTURE_PORTAL_ORDERS -> PORTAL_ORDERS`, and `FIXTURE_STRIPE_EVENTS -> PORTAL_STRIPE_EVENTS`, then cleared `PORTAL_EMAIL_QUEUE`. Fixture-storage tabs were not mutated. The daily scheduler function is deployed but not installed/activated because active runtime tabs remain fixture-loaded after the version `993` live-suite reset; installing the trigger should wait until active tabs are restored or the owner explicitly authorizes fixture-mode scheduler activation.
- Repository: `red-threads/redthreads-ost-portal`.
- Local path: `/Users/Josiah/Documents/GitHub/redthreads-ost-portal`.
- Local fixture/test-harness tooling includes redacted email-review fixture audit and lifecycle communication matrix validation commands. The 2026-06-19 controlled fixture-storage normalization mutated only `FIXTURE_EXPORT`, `FIXTURE_PORTAL_ORDERS`, and `FIXTURE_STRIPE_EVENTS`: headers remained compatible, `FIXTURE_EXPORT` embedded historical header rows were cleared, repeated active-order references dropped from 98 to 7, then the second storage buildout added safe dedicated lifecycle rows for card paid/failed, manual received, AP ACH paid/failed, PO invoice prepared, PO payment received, production complete, and team-initiated production before payment. `FIXTURE_EXPORT` and `FIXTURE_PORTAL_ORDERS` now have 16 aligned order-context fixture rows, and `FIXTURE_STRIPE_EVENTS` now has 9 payment-event rows. A later controlled active reset copied only `FIXTURE_EXPORT -> EXPORT_LOG`, `FIXTURE_PORTAL_ORDERS -> PORTAL_ORDERS`, and `FIXTURE_STRIPE_EVENTS -> PORTAL_STRIPE_EVENTS` for dry-run validation after a private Drive backup; `PORTAL_EMAIL_QUEUE`, users/sessions/accounts, Script Properties, and Apps Script config were not touched in that reset. Version `982` deploys the review-harness fixture-selection/metadata patch so protected dry run validates the active matrix, and version `983` adds assertion coverage for `team_initiated_production_before_payment` and `production_complete`. The live owner review-suite blast on version `983` used the suite's default non-dry-run reset path, re-copied `FIXTURE_EXPORT -> EXPORT_LOG`, `FIXTURE_PORTAL_ORDERS -> PORTAL_ORDERS`, and `FIXTURE_STRIPE_EVENTS -> PORTAL_STRIPE_EVENTS`, cleared `PORTAL_EMAIL_QUEUE` rows, sent 15 team-recipient review emails, skipped 32 configured/omitted cases, failed 0, reported 13 known fixture attachment fallbacks, and reported 0 lifecycle contradiction warnings/errors.
- Runtime root: `apps-script/src/`.
- Tracked runtime files: `Code.js`, `Index.html`, `TaxForm3372Manifest.html`, `appsscript.json`, `.clasp.json`.
- Current tracked app has been pulled from the live Apps Script project and includes the fuller portal architecture: auth shell, dashboard/order lifecycle surfaces, Stripe checkout routing, Team Mode lanes, and tax-form manifest support.
- Tracked runtime source now includes same-window Stripe Checkout launch helpers, ACH bank transfer checkout selection, ACH payment-mode Checkout sessions that omit `customer` and `customer_email` by default so Stripe collects payer contact email each time, an ACH pre-checkout decision step with generic Stripe-hosted ACH copy and AP payment-link/email lanes in a separate modal container, one top-level `ACH payment accounts` dashboard card that opens the Manage ACH banks modal with a full-card pointer cursor, modal-local ACH verification guidance and dashboard-route-independent close handling, server-only Stripe test-mode microdeposit verification fallback after hosted-link lookup with descriptor-code/amount-mode handling, standard order-checkout ACH lifecycle client emails plus team alerts queued through `PORTAL_EMAIL_QUEUE` and rendered through a shared lifecycle email shell/progress snapshot, modal-local green ACH setup loading transition, silent Dashboard ACH setup cancel returns, body-mounted Manage ACH banks backdrop/outside-click handling, dashboard tax document viewer close/outside-click handling, approved Credit Terms document viewer document layout with restored Submission Details rail, account-dashboard direct links, tokenized-dashboard ACH setup authorization, Stripe setup return validation that tolerates Checkout's `{CHECKOUT_SESSION_ID}` placeholder, client-side dashboard/project route synchronization for durable parent URLs, dashboard-saved versus order-only/AP ACH bank scoping, AP public ACH payment links with awaiting-AP display semantics and rendered Summary/Invoice email attachment requirements, locked AP/ACH invoice rendering from durable order-draft summaries, ACH/AP in-flight Summary control suppression, dashboard-origin project open Team Mode inheritance hardening, direct account-dashboard link identity/project-binding validation, explicit Team Mode route preservation across the Squarespace wrapper and Apps Script sandbox frames, deployed communication-tranche queue coverage for ACH/AP/card/PO/manual payments, portal lifecycle/admin/account-document events, 10-minute chat digests, bounded portal-traffic queue nudging for due email jobs, portal load timing instrumentation, account dashboard project caching, dashboard full-project prefetch, cached safe project-to-dashboard returns, lazy tax-form manifest loading, account-document upload deeplinks that start dashboard project hydration in the background and show a dashboard loading row if closed before rows render, a global project-entry-style checkout transition overlay with browser-back state restoration, checkout-aware unsaved-change suppression during approved Stripe navigation, checkout-return welcome modal suppression through server-rendered request-route metadata, checkout-return client-mode routing that prevents stale Team Mode inheritance after Stripe returns, structured checkout launch timing instrumentation with flat copyable client/server summary lines, a fast normal-checkout response path that skips noncritical response hydration before Stripe navigation, optimized competing-unpaid-order supersede row writes with timing counters, desktop-only mobile-blocking UI with a centered ellipsized copy-link display, login/reset autofill styling that preserves browser saved credentials without the pale browser autofill field repaint, password-reset return routes that resolve the reset email before first paint and lock that email on the reset-code view, dashboard project rows that keep a default cursor on non-interactive project detail/subtext copy and use fixed table geometry plus a two-line project-name cap so long names cannot compress the Order Progress rail or Open Project column, Manage Accounts for ACH Payments modal copy with a client-visible soft Remove action for dashboard-saved ACH accounts and centered modal footer actions, a simplified account-dashboard ACH payment accounts card with only the Manage accounts action text, doPost route parity for credit-terms team review/approval, purchase-order submit, and chat smoke actions with Team sender auth enforced, an owner-only Team Mode authorized `send_email_review_suite` route that resets from `FIXTURE_*` tabs and sends redacted inbox-review examples of active email families, updated blank credit-terms and blank tax source email green callouts/instructions/CTA handling, a Portal Native estimate-copy sender review panel above the project-name row, credit-terms/tax-exemption account-document client emails with explicit blank-copy handling so removed status/next-step/no-action objects stay removed, tax-exemption denied client emails mirror the credit-terms denied structure with tax-specific not-approved language and a `Resubmit Sales Tax Exemption Form` CTA, tax-exemption approved client emails now show the sales-tax toggle capability in the green no-action panel and omit the old approved-status sentence, credit-terms approved client emails now show purchase-order capability in the green no-action panel, then the Details block, then the Net terms payment paragraph, tax-exemption reset emails use the Sales Tax Exemption resubmission copy and `Complete Sales Tax Exemption Form` CTA, PO submitted and PO payment received client emails use the updated invoice/receipt, portal availability, payment-timing copy, and production-started PO submitted header, PO invoice prepared emails with the updated Purchasing/AP purchase-order next-action copy rendered all blue with no red `portal link` emphasis, review-suite eligibility controls with all configured recipient classes currently unlocked, payment receipt emails that guard against stale payment-required next actions, manual payment pending client emails with the updated order-placed/payment-required header, check/cash payment instructions, and all-blue payment next-action line, card-failed client emails with credit-card-specific retry copy, production-cannot-begin copy, and blocked-payment production timing suppression, Standard ACH receipt/failed/verification/pending client emails with the updated production/payment copy, production-started or production-not-started headers, and all-blue failed next action, AP payment-link emails with requester/project headings, first-name purchaser notes, purchaser-copied payment request copy, and phone verification guidance, AP ACH pending/receipt/failed AP emails with the updated owner-facing copy and production-started receipt header, no pending AP status CTA, and centered AP ACH failed AP retry CTA, card-paid client emails with the updated payment-received/production-started copy, team-facing emails with configured Team Mode password hints below Team Mode buttons plus signed 30-day `teamAccess` auto-unlock links, normalized Team Mode project CTA labels, client-to-team chat digests that place a red portal-messages label and client-named messages/reply CTA directly before lifecycle progress/details without the duplicate project summary card, account-document team notifications that include client name, client email, and organization details, credit-terms and tax-exemption submitted team review emails that place Details directly under the header before the combined review instruction and centered Team Review CTA/password, team project-detail cards that pair project details with client details in a compact two-column body, pending Standard ACH/AP ACH team alerts that render green no-action labels with muted optional production-exception copy while preserving the project-details Team Mode link, email progress that prefers canonical placed-order lifecycle readiness over stale export defaults, chat digest persistence/rendering that preserves selected print-job context as `PJ#` lines while stripping embedded `Comment on Print Job...` classifier copy from digest message bodies, chat digest email content now carries lifecycle communication contract metadata for review/assertion parity, team email attachment notes that refer to the client's order instead of `your order`, client-to-team chat digest message body text rendered in green for client messages, review-suite document-copy metadata for password reset, summary/invoice explicit send, blank tax source, and blank credit-terms source examples, team action intro copy normalization away from client possessive wording, and a bottom-left glass Red Threads revision badge showing `113`.
- Version `982` deploys the active-fixture review-harness repair from commit `f9da100`. The suite now selects dedicated active fixture rows for card paid/failed, AP ACH pending/paid/failed, manual pending/received, and PO submitted/payment-received examples, and account-document lifecycle emails report account-document communication intent metadata instead of inherited order payment intent. `Index.html` was not changed and still shows revision `111`. `clasp version` created `982`, the existing stable deployment ID was updated at `@982 - Fix email review fixture intent selection`, direct Apps Script `/exec` returned HTTP 200 with `Development revision 111`, public `/portal` returned HTTP 200 and still referenced the stable deployment ID, and targeted secret-marker checks returned zero hits for client secrets, hosted verification URLs, portal token query URLs, token assignments, checkout URLs, and Drive file URLs. The protected headless owner email-review dry run returned `ok:true` with 0 sent, 32 skipped, 0 failed, 13 known fixture attachment fallbacks, and 0 lifecycle-communication warnings/errors. Matrix validation returned `ok:true`; the previous four intent mismatches cleared, leaving only the two expected assertion-only gaps.
- Version `983` deploys the post-hardening lifecycle communication assertion patch from commit `01af15e`. The communication contract now has explicit `production_complete` intent handling plus safe production-active copy for team-initiated production before payment, and the protected dry-run assertion matrix now covers `team_initiated_production_before_payment` and `production_complete`. `Index.html` was not changed and still shows revision `111`. `clasp version` created `983`, the existing stable deployment ID was updated at `@983 - Harden lifecycle communication assertions`, direct Apps Script `/exec` returned HTTP 200 with `Development revision 111`, public `/portal` returned HTTP 200 and still referenced the stable deployment ID, and targeted secret-marker checks found no client secrets, hosted verification URLs, raw tokenized portal URLs, private URLs, full checkout URLs, or Drive URLs. The protected headless owner email-review dry run returned `ok:true` with 0 sent, 32 skipped, 0 failed, 13 known fixture attachment fallbacks, and 0 lifecycle-communication warnings/errors. Matrix validation returned `ok:true` with 29 required, 18 covered, 11 skipped/omitted, 0 missing, and 0 intent mismatches. The live owner review-suite blast on version `983` returned `ok:true`, sent 15, skipped 32, failed 0, reported 13 known fixture attachment fallbacks, and reported 0 lifecycle contradiction warnings/errors. The live blast did not deploy Apps Script or mutate fixture tabs, but it did run the suite's default active fixture reset and queue-clear path.
- Version `984` unlocks all configured owner email-review suite fixtures by clearing both the label-specific omission map and the client recipient-class omission map. `Index.html` shows revision `112`. `clasp version` created `984`, the existing stable deployment ID was updated at `@984 - Unlock all email review fixtures`, direct Apps Script `/exec` returned HTTP `200` with `Development revision 112` and omitted stale revision `111`, public `/portal` returned HTTP `200` and still referenced the stable deployment ID with route bridge and `teamAccess` passthrough markers, and targeted secret-marker checks found no client secrets, hosted verification URLs, or raw tokenized portal URLs. No owner email-review dry run or live send was run during this ship.
- Version `985` closes the unlocked owner email-review suite gate by adding document-copy communication metadata to the password reset, summary/invoice explicit send, blank tax source, and blank credit-terms source review rows, and by normalizing team locked-order intro copy away from client possessive wording. `Index.html` shows revision `113`. `clasp version` created `985`, the existing stable deployment ID was updated at `@985 - Fix email review matrix metadata`, direct Apps Script `/exec` returned HTTP `200` with `Development revision 113` and omitted stale revision `112`, public `/portal` returned HTTP `200` and still referenced the stable deployment ID with route bridge and `teamAccess` passthrough markers, and targeted secret/private-link checks found zero actual sensitive markers. The protected dry run returned `ok:true`, sent `0`, skipped `1`, failed `0`, reported `30` attachment fallbacks, `0` contradiction warnings/errors, and matrix `ok:true` with `29` required, `29` covered, `0` skipped/missing, and `0` intent mismatches. The owner-approved live suite returned `ok:true`, sent `46` review emails (`23` client, `19` team, `4` AP), skipped only `Submitted tax-form copy client` because the submitted artifact is unavailable in fixtures, failed `0`, reported `30` attachment fallbacks, and reported `0` contradiction warnings/errors. The live non-dry-run path reset `FIXTURE_EXPORT -> EXPORT_LOG`, `FIXTURE_PORTAL_ORDERS -> PORTAL_ORDERS`, and `FIXTURE_STRIPE_EVENTS -> PORTAL_STRIPE_EVENTS`, then cleared `PORTAL_EMAIL_QUEUE`; fixture-storage tabs were not mutated.
- On 2026-06-22, a fresh full owner email-review suite blast was run against stable Apps Script version `985` after a clean protected dry run. The dry run returned `ok:true`, sent `0`, skipped `1`, failed `0`, total `59`, attachment fallback `30`, contradiction warnings/errors `0`, and strict matrix `ok:true` with `29 / 29` covered and `0` intent mismatches. The live blast returned `ok:true`, sent `46` review emails (`23` client, `19` team, `4` AP), skipped only `Submitted tax-form copy client` because the submitted artifact is unavailable, failed `0`, attachment matched `0`, attachment fallback `30`, and contradiction warnings/errors `0`. No code changed and no Apps Script deployment was performed. The live non-dry-run path again reset `FIXTURE_EXPORT -> EXPORT_LOG`, `FIXTURE_PORTAL_ORDERS -> PORTAL_ORDERS`, and `FIXTURE_STRIPE_EVENTS -> PORTAL_STRIPE_EVENTS`, then cleared `PORTAL_EMAIL_QUEUE`; fixture-storage tabs were not mutated.
- Version `986` narrows the owner email-review suite by hiding owner-reviewed/pass labels from future suite sends after applying the requested copy/style refinements. Standard ACH pending/verification/receipt and manual-payment-received client emails now say `invoice/receipt`; Standard ACH failed client copy now says `contact Red Threads for assistance` and includes a centered red dynamic `Click to open Project #... to retry payment` action-card button; AP ACH failed AP next action uses `assistance`; and the team-to-client chat digest renders the sender/time, PJ line, and reply text in distinct reds with the reply text most prominent. `Index.html` shows revision `114`. `clasp version` created `986`, the existing stable deployment ID was updated at `@986 - Refine reviewed email suite omissions`, direct Apps Script `/exec` returned HTTP `200` with `Development revision 114` and omitted stale revision `113`, public `/portal` returned HTTP `200` and still referenced the stable deployment ID, and targeted secret/private-link checks found zero actual sensitive markers. The protected dry run returned `ok:true`, sent `0`, skipped `26`, failed `0`, reported `16` attachment fallbacks, `0` contradiction warnings/errors, and non-strict matrix `ok:true` with `29` required, `21` covered, `8` skipped/omitted, `0` missing, and `0` intent mismatches. No live review-suite blast, active-tab mutation, fixture-tab mutation, or `PORTAL_EMAIL_QUEUE` clear was performed in this pass.
- Version `987` adds a centered red dynamic retry button to the `Card failed client` action card. Payment lifecycle failure client emails now label the action-card CTA `Click to access Project #... and retry payment` and continue routing to the existing invoice-summary project portal URL. `Card failed client` is now hidden from future review-suite sends through the owner-reviewed omission map. `Index.html` shows revision `115`. `clasp version` created `987`, the existing stable deployment ID was updated at `@987 - Add card failed retry CTA`, direct Apps Script `/exec` returned HTTP `200` with `Development revision 115` and omitted stale revision `114`, public `/portal` returned HTTP `200` and still referenced the stable deployment ID, and targeted secret/private-link checks found zero actual sensitive markers. The protected dry run returned `ok:true`, sent `0`, skipped `27`, failed `0`, reported `15` attachment fallbacks, `0` contradiction warnings/errors, and non-strict matrix `ok:true` with `29` required, `21` covered, `8` skipped/omitted, `0` missing, and `0` intent mismatches. No live review-suite blast, active-tab mutation, fixture-tab mutation, or `PORTAL_EMAIL_QUEUE` clear was performed in this pass.
- Version `988` adds shared email production timing display. Client-facing order-context Project Details cards now insert `Production Time` immediately after `Project total` when selected jobs include a parseable business-day turnaround; non-order client emails omit the row. Shared client/team email progress now renders active production as `In Production` with `Will Finish M/D` when lifecycle-derived production target timing is reliable, and completed production as `Production` with `Completed M/D`, preferring actual all-job completion dates before lifecycle/timeline completion dates. `Index.html` shows revision `116`. `clasp version` created `988`, the existing stable deployment ID was updated at `@988 - Add email production timing`, direct Apps Script `/exec` returned HTTP `200` with `Development revision 116` and omitted stale revision `115`, public `/portal` returned HTTP `200` and still referenced the stable deployment ID, and targeted secret/private-link checks found zero actual sensitive markers. The protected post-deploy dry run returned `ok:true`, sent `0`, skipped `27`, failed `0`, total `59`, attachment fallback `15`, contradiction warnings/errors `0`, and non-strict matrix `ok:true` with `29` required, `21` covered, `8` skipped/omitted, `0` missing, and `0` intent mismatches. No live review-suite blast, active-tab mutation, fixture-tab mutation, or `PORTAL_EMAIL_QUEUE` clear was performed in this pass.
- Version `989` ships the client/team email parity cleanup and full review-suite unlock. Team order-context Project Details now use the same `Production Time` row as client order-context Project Details when selected jobs include parseable business-day turnaround data; AP ACH receipt history now replaces the shared generic `ACH payment received` line with `AP ACH payment received` instead of rendering both; and `EMAIL_REVIEW_SUITE_OMITTED_LABELS_` is cleared again. `Index.html` shows revision `117`. `clasp version` created `989`, the existing stable deployment ID was updated at `@989 - Unify email timing and review suite`, direct Apps Script `/exec` returned HTTP `200` with `Development revision 117` and omitted stale revision `116`, public `/portal` returned HTTP `200` and still referenced the stable deployment ID, and targeted checks found no actual secrets or private links. The protected post-deploy dry run returned `ok:true`, sent `0`, skipped `1`, failed `0`, total `59`, attachment fallback `30`, contradiction warnings/errors `0`, and strict matrix `ok:true` with `29 / 29` covered and `0` intent mismatches. The owner-approved live suite returned `ok:true`, sent `46` emails (`23` client, `19` team, `4` AP), skipped only `Submitted tax-form copy client`, failed `0`, attachment fallback `30`, and contradiction warnings/errors `0`. The live non-dry-run path reset `FIXTURE_EXPORT -> EXPORT_LOG`, `FIXTURE_PORTAL_ORDERS -> PORTAL_ORDERS`, and `FIXTURE_STRIPE_EVENTS -> PORTAL_STRIPE_EVENTS`, then cleared `PORTAL_EMAIL_QUEUE`; fixture-storage tabs were not mutated.
- Version `990` hides the owner-reviewed/pass email labels requested after inbox review and refines the remaining action-card copy before hiding those labels. `Explicit locked-order resend client` now uses `invoice/receipt` wording in the client intro, next action, and attachment sentence. `PO invoice prepared client` now uses a centered red action-card CTA labeled `Click to access project #... and upload P.O.` that routes to the existing PO resume/upload URL. `PO submitted client` and `Manual payment pending client` now use centered red action-card CTAs labeled `Click to access project #... and make a payment.` that route to the existing Summary/Invoice project URL. The review-suite omission map now skips the requested reviewed client/AP labels with `owner_reviewed_hidden` while preserving production communication policy. `Index.html` shows revision `118`. `clasp version` created `990`, the existing stable deployment ID was updated at `@990 - Hide reviewed email suite labels and refine CTAs`, direct Apps Script `/exec` returned HTTP `200` with `Development revision 118` and omitted stale revision `117`, public `/portal` returned HTTP `200` and still referenced the stable deployment ID, and targeted secret/private-link checks found zero actual sensitive markers. Per owner instruction, no email-review dry run or live suite was run. Active runtime tabs, fixture-storage tabs, and `PORTAL_EMAIL_QUEUE` were not mutated in this pass.
- Version `991` enables the all-jobs production-complete lifecycle email while keeping partial `jobs_completed` updates policy-disabled. Team Mode job completion now uses a stable `project_completed_{orderId}_{selectedJobIds}` event key when every selected job is complete, so completion-date edits do not duplicate-send the same project-complete notice. Production-complete portal lifecycle emails render through the existing lifecycle email shell for both client and team recipients, use fulfillment-aware client copy for `ready to ship via UPS Ground` or `ready for pickup at Red Threads, 505 South Saginaw Road, Midland, Michigan 48640`, and do not claim a tracking link or invoice/receipt attachment. The review suite now has sendable `Production complete shipping client/team` and `Production complete pickup client/team` cases, and the matrix requires those labels for `production_complete` coverage instead of assertion-only rows. `Index.html` shows revision `119`. `clasp version` created `991`, the existing stable deployment ID was updated at `@991 - Add production complete emails`, direct Apps Script `/exec` returned HTTP `200` with `Development revision 119` and omitted stale revision `118`, public `/portal` returned HTTP `200` and still referenced the stable deployment ID, and targeted secret-marker checks found zero hits. The protected post-deploy dry run returned `ok:true`, total `63`, skipped `27`, failed `0`, sent `0`, attachment fallback `15`, contradiction warnings/errors `0`, and non-strict matrix `ok:true` with `29` required, `21` covered, `8` skipped/omitted, `0` missing, and `0` intent mismatches. No live review-suite blast was run; the dry run did not reset active runtime tabs, touch fixture-storage tabs, clear `PORTAL_EMAIL_QUEUE`, or send emails.
- Version `992` refines team email action-card status labels. Team order-context emails using the shared lifecycle shell now show exactly `Action required` in red, `Potential action required` in blue, or `No action required` in green as the action-card title, while preserving the existing team intro, status copy, next-step copy, Team Mode CTA, password hint, lifecycle contract metadata, recipient routing, queue behavior, idempotency, and attachment policy. The owner review-suite omission map now also hides `Explicit locked-order resend team`, `Chat digest client to team`, `Credit terms approved team`, `Credit terms submitted team review`, `Tax exempt approved team`, and `Tax exempt submitted team review`. `Index.html` shows revision `120`. `clasp version` created `992`, the existing stable deployment ID was updated at `@992 - Refine team email action status`, direct Apps Script `/exec` returned HTTP `200` with `Development revision 120` and omitted stale revision `119`, public `/portal` returned HTTP `200` and still referenced the stable deployment ID, and targeted secret-marker checks found zero hits. The protected post-deploy dry run returned `ok:true`, total `63`, skipped `33`, failed `0`, sent `0`, attachment fallback `14`, contradiction warnings/errors `0`, and non-strict matrix `ok:true` with `29` required, `16` covered, `13` skipped/omitted, `0` missing, and `0` intent mismatches. The owner-approved live suite returned `ok:true`, sent `18` emails, skipped `33`, failed `0`, attachment fallback `14`, and contradiction warnings/errors `0`. The live non-dry-run path reset `FIXTURE_EXPORT -> EXPORT_LOG`, `FIXTURE_PORTAL_ORDERS -> PORTAL_ORDERS`, and `FIXTURE_STRIPE_EVENTS -> PORTAL_STRIPE_EVENTS`, then cleared `PORTAL_EMAIL_QUEUE`; fixture-storage tabs were not mutated.
- Version `981` adds lifecycle communication contract metadata to the active chat digest email builder. Chat digest copy and routing are unchanged, but generated client/team digest content now carries a `chat_digest` contract so review tooling can report intent, lifecycle state, payment disposition, production disposition, CTA mode, and contradiction counts when the fixtures are enabled or inspected directly. `Index.html` shows revision `111`. `clasp version` created `981`, the existing stable deployment ID was updated at `@981 - Add chat digest communication metadata`, direct Apps Script `/exec` returned HTTP 200 with `Development revision 111` and omitted stale revision `110`, public `/portal` returned HTTP 200 and still referenced the stable deployment ID with `teamAccess` passthrough, and targeted secret-marker checks returned zero hits. The headless owner email-review dry run returned `ok:true` with 0 sent, 32 skipped, 0 failed, 13 fixture attachment fallbacks, and 0 lifecycle-communication warnings/errors; live review emails were not sent because this was metadata-only with no visible email rendering change.
- Version `980` refines team lifecycle attachment assertions. The shared lifecycle email shell now passes recipient class into attachment-note normalization, so team-recipient emails that carry optional invoice/status attachments render `An invoice/status PDF for the client's order may be attached when available.` instead of falling back to client-facing `your order` copy. `Index.html` shows revision `110`. `clasp version` created `980`, the existing stable deployment ID was updated at `@980 - Refine team lifecycle attachment assertions`, direct Apps Script `/exec` returned HTTP 200 with `Development revision 110` and omitted stale revision `109`, public `/portal` returned HTTP 200 and still referenced the stable deployment ID with `teamAccess` passthrough, and targeted secret-marker checks returned zero hits. The headless owner email-review dry run returned `ok:true` with 0 sent, 32 skipped, 0 failed, 13 fixture attachment fallbacks, and 0 lifecycle-communication warnings/errors; the live suite returned `ok:true` with 15 sent, 32 skipped, 0 failed, 13 fixture attachment fallbacks, and 0 lifecycle-communication warnings/errors. Attachment fallbacks remain the known fixture `artifact_project_mismatch` condition.
- Version `979` deploys Lifecycle Communication Contract V1. Direct locked-order confirmation/resend, purchase-order invoice prepared/send, AP payment-link, and order-context summary/invoice/document sends now resolve lifecycle-derived communication intent, payment disposition, production disposition, CTA mode, and safe copy before rendering or asserting content. Severe direct-send contradictions hard-block; review-suite dry run now renders no-send cases with redacted communication metadata plus assertion-only lifecycle matrix cases. `Index.html` shows revision `109`. `clasp version` created `979`, the existing stable deployment ID was updated at `@979 - Add lifecycle communication contract`, direct Apps Script `/exec` returned HTTP 200 with `Development revision 109` and omitted stale revision `108`, public `/portal` returned HTTP 200 and still referenced the stable deployment ID with `teamAccess` passthrough, and targeted secret-marker checks returned zero hits. The headless owner email-review dry run returned `ok:true` with 0 sent, 32 skipped, 0 failed, and 13 fixture attachment fallbacks; the live suite sent 15, skipped 32, failed 0, and reported 13 attachment fallbacks. At version `979`, three advisory `team_client_possessive_language` warnings remained in queued failed-payment team fixture outputs; version `980` resolves those warnings. Direct utility contract assertions reported zero errors.
- Version `978` omits the validated `Explicit locked-order resend team` and `Chat digest client to team` fixtures from the owner email review suite and changes client message body text in the client-to-team chat digest email to the shared green success color. `Index.html` shows revision `108`. `clasp version` created `978`, the existing stable deployment ID was updated at `@978 - Refine chat review suite omissions`, direct Apps Script smoke returned HTTP 200 with `Development revision 108`, public `/portal` returned HTTP 200 and still referenced the stable deployment ID, and targeted secret-marker checks returned zero hits. The owner email review suite was intentionally not triggered per owner instruction.
- Version `977` refines team email header language. Team-facing lifecycle, payment, AP ACH, Standard ACH, account-document review, locked-order resend, and client-to-team chat digest email surfaces now render `Red Threads Portal - Team Notification` as the visible red eyebrow, while client/AP/public emails continue to render `Red Threads Portal - Notification`. Team email subject/heading copy sources no longer include `Team alert:` prefixes; operational action labels such as `Team action required` remain available in body cards. `Index.html` shows revision `107`. `clasp version` created `977`, the existing stable deployment ID was updated at `@977 - Refine team email headers`, direct Apps Script smoke returned HTTP 200 with `Development revision 107`, public `/portal` returned HTTP 200 and still referenced the stable deployment ID, and targeted secret-marker checks returned zero hits. The headless owner email review-suite dry run returned `ok:true`; the real suite sent 17 emails, skipped 30 validated/omitted fixtures, reported 0 failures, and reported 14 attachment fallbacks due to the known fixture `artifact_project_mismatch` state.
- Version `976` refines team-facing attachment copy. Team Standard ACH, AP ACH, card, manual payment, PO, and locked-order resend attachment notes now say `The invoice/receipt for the client's order is attached to this email.` instead of client-facing `your order` or short `your receipt` wording, and the shared attachment sentence normalizer preserves that team-specific phrase. `Index.html` shows revision `106`. `clasp version` created `976`, the existing stable deployment ID was updated at `@976 - Refine team attachment copy`, direct Apps Script smoke returned HTTP 200 with `Development revision 106`, public `/portal` returned HTTP 200 and still referenced the stable deployment ID, and targeted secret-marker checks returned zero hits. The headless owner email review-suite dry run returned `ok:true`; the real suite sent 17 emails, skipped 30 validated/omitted fixtures, reported 0 failures, and reported 14 attachment fallbacks due to the known fixture `artifact_project_mismatch` state.
- Version `975` adds selected print-job context to the active chat digest emails. `appendChatMessage`, `createChatMessage_`, and `normalizeChatLog_` now preserve `pjId`, `pjName`, and `pjLabel`; client and team digest renderers now display sender/date, `PJ#` job context, and clean message text while stripping the embedded `Comment on Print Job...` classifier line; and chat review fixtures include representative job metadata for both client-to-team and team-to-client directions. `Index.html` shows revision `105`. `clasp version` created `975`, the existing stable deployment ID was updated at `@975 - Add chat digest print job context`, direct Apps Script smoke returned HTTP 200 with `Development revision 105`, public `/portal` returned HTTP 200 and still referenced the stable deployment ID, and targeted secret-marker checks returned zero hits. The owner email review suite was intentionally not triggered per owner instruction.
- Version `956` refines the Standard ACH and AP payment-link email surfaces and ships the canonical email progress repair. The owner review-suite now omits the validated Standard ACH pending, verification, receipt, and failed client fixtures; Standard ACH receipt client copy now combines payment received with production begin copy; Standard ACH failed client copy now states production has not started and directs users to the portal/project retry path; AP payment-link emails now show the purchaser/project request heading, first-name purchaser note label, and invoice-attached secure-payment copy. Email progress now avoids stale export defaults by treating placed-order lifecycle readiness as canonical when a current order row exists. `clasp version` created `956`, the existing stable deployment ID was updated at `@956 - Refine ACH and AP payment emails`, direct Apps Script smoke returned HTTP 200 with `Development revision 86`, public `/portal` returned HTTP 200 and still referenced the stable deployment ID, and targeted secret-marker checks returned zero hits. The headless owner email review-suite dry run returned `ok:true`; the real suite sent 25 emails, skipped 22 validated/omitted items, reported 0 failures, and reported 20 attachment fallbacks due to the known fixture `artifact_project_mismatch` state.
- Version `957` refines AP ACH pending/receipt AP emails and ships the AP payment-link contact-name fallback repair. AP ACH receipt AP emails now use `Payment Received, Order Started` and a single no-action payment/production/attachment sentence. AP ACH pending AP emails now use `ACH Payment Pending`, combine the payment-pending, invoice-attached, production-not-started, and account/project-owner update copy into the action card, use the updated Stripe bank-verification next action, and no longer render the `View AP payment status` CTA. AP payment-link emails now resolve the purchaser name from order draft, order-specific account, account summary, row contact fields, and identity fallback, while new order drafts store account contact names. `clasp version` created `957`, the existing stable deployment ID was updated at `@957 - Refine AP ACH payment emails`, direct Apps Script smoke returned HTTP 200 with `Development revision 87`, public `/portal` returned HTTP 200 and still referenced the stable deployment ID, and targeted secret-marker checks returned zero hits. The owner email review suite was intentionally not triggered per owner instruction.
- Version `958` refines AP ACH failed AP and card-paid client emails. AP ACH failed AP emails now use `ACH Payment Issue, Order Paused`, explain that Stripe could not complete the Accounts Payable ACH payment, keep the invoice-reference/payment-required copy in the action card, and direct the recipient to return to the Portal/Project retry button. Card paid client emails now use `Payment Received, Order Started` and a single no-action payment/production/invoice-receipt sentence, and the owner review-suite now omits the validated `Card paid client` and `PO invoice prepared client` fixtures. `clasp version` created `958`, the existing stable deployment ID was updated at `@958 - Refine AP ACH failed and card paid emails`, direct Apps Script smoke returned HTTP 200 with `Development revision 88`, public `/portal` returned HTTP 200 and still referenced the stable deployment ID, and targeted secret-marker checks returned zero hits. The headless owner email review-suite dry run returned `ok:true`; the real suite sent 23 emails, skipped 24 validated/omitted items, reported 0 failures, and reported 18 attachment fallbacks due to the known fixture `artifact_project_mismatch` state.
- Version `959` omits the validated AP payment-link/AP ACH AP review fixtures and centers the AP ACH failed AP retry CTA. The owner review-suite now skips `AP payment link sent`, `AP ACH pending AP`, `AP ACH receipt AP`, and `AP ACH failed AP`; team AP ACH review fixtures remain sendable. AP ACH failed AP emails now pass a centered CTA alignment hint through the shared lifecycle action-card renderer, so the retry button centers inside the action container. `clasp version` created `959`, the existing stable deployment ID was updated at `@959 - Omit AP review emails and center AP ACH failed CTA`, direct Apps Script smoke returned HTTP 200 with `Development revision 89`, public `/portal` returned HTTP 200 and still referenced the stable deployment ID, and targeted secret-marker checks returned zero hits. The headless owner email review-suite dry run returned `ok:true`; the real suite sent 19 emails, skipped 28 validated/omitted items, reported 0 failures, and reported 14 attachment fallbacks due to the known fixture `artifact_project_mismatch` state.
- Version `960` turns all configured owner email review-suite fixtures back on by clearing `EMAIL_REVIEW_SUITE_OMITTED_LABELS_`. `clasp version` created `960`, the existing stable deployment ID was updated at `@960 - Enable all email review suite fixtures`, direct Apps Script smoke returned HTTP 200 with `Development revision 90`, public `/portal` returned HTTP 200 and still referenced the stable deployment ID, and targeted secret-marker checks returned zero hits. The headless owner email review-suite dry run returned `ok:true`; the real suite sent 46 emails, skipped 1 fixture-missing submitted-tax-copy item, reported 0 failures, and reported 30 attachment fallbacks due to the known fixture `artifact_project_mismatch` state.
- Version `961` refines account-document review email copy and green callout styling. Blank credit-terms source emails now place the Credit Application/Purchase Order capability message in the green callout under the header, remove the old blank-PDF-attached sentence, and use the updated account/application review instruction. Blank tax document source emails now place the sales-tax toggle capability message in the same green callout, remove the old blank-PDF-attached sentence, and use white upload-instruction copy. Credit terms approved client emails now use a green Purchase Order capability callout plus the Purchase Order production/payment-terms paragraph before Details. `clasp version` created `961`, the existing stable deployment ID was updated at `@961 - Refine account document review emails`, direct Apps Script smoke returned HTTP 200 with `Development revision 91`, public `/portal` returned HTTP 200 and still referenced the stable deployment ID, and targeted secret-marker checks returned zero hits. The headless owner email review-suite dry run returned `ok:true`; the real suite sent 46 emails, skipped 1 fixture-missing submitted-tax-copy item, reported 0 failures, and reported 30 attachment fallbacks due to the known fixture `artifact_project_mismatch` state.
- Version `962` refines payment lifecycle review email copy and styling. Manual payment received, card paid, AP ACH receipt AP, and Standard ACH receipt client emails now use production-started header wording. Card failed client emails now state that order production cannot begin until the credit card payment is completed and the next action says retrying payment begins order production. Standard ACH verification client copy now states production begins when payment is received. Standard ACH failed client next-action text renders entirely in blue while preserving the existing words. `clasp version` created `962`, the existing stable deployment ID was updated at `@962 - Refine payment lifecycle review emails`, direct Apps Script smoke returned HTTP 200 with `Development revision 92`, public `/portal` returned HTTP 200 and still referenced the stable deployment ID, and targeted secret-marker checks returned zero hits. The headless owner email review-suite dry run returned `ok:true`; the real suite sent 46 emails, skipped 1 fixture-missing submitted-tax-copy item, reported 0 failures, and reported 30 attachment fallbacks due to the known fixture `artifact_project_mismatch` state.
- Version `963` refines AP payment-link request email copy. AP payment-link emails now tell Accounts Payable that a member of their organization requested payment, name the resolved purchaser/contact, state that the purchaser is copied, preserve invoice-attached secure-payment and final invoice/receipt update copy, remove the old comma before assistance, and add `989-600-8540` as the Red Threads phone verification option. `clasp version` created `963`, the existing stable deployment ID was updated at `@963 - Refine AP payment request email copy`, direct Apps Script smoke returned HTTP 200 with `Development revision 93`, public `/portal` returned HTTP 200 and still referenced the stable deployment ID, and targeted secret-marker checks returned zero hits. The owner email review suite was intentionally not triggered per owner instruction.
- Version `964` enhances team email actions and Team Mode deep links. Team-facing order-context emails now resolve operational action labels, next-step copy, centered Team Mode CTAs, and `teamAction` deep-link intents from canonical lifecycle/team workflow state; `initiate_production` is explicitly handled; Team Mode passwords are no longer rendered in email text or HTML; client-to-team chat digests include lifecycle progress/details; locked-order resend team review no longer reuses client payment-option framing; account-document team review notifications use the shared dark panel/action-card treatment; and `teamAction` route metadata is preserved through `doGet`, VM boot metadata, wrapper route replacement, and post-unlock Team Controls focus without executing state-changing actions. `Index.html` shows revision `94`. `clasp version` created `964`, the existing stable deployment ID was updated at `@964 - Enhance team email actions`, direct Apps Script smoke returned HTTP 200 with `Development revision 94`, public `/portal` returned HTTP 200 and still referenced the stable deployment ID, targeted secret-marker checks returned zero hits, and direct `teamAction` plus `teamReview` route smokes returned HTTP 200 with Team Mode source present. The headless owner email review-suite dry run returned `ok:true`; the real suite sent 46 emails, skipped 1 fixture-missing submitted-tax-copy item, reported 0 failures, and reported 30 attachment fallbacks due to the known fixture `artifact_project_mismatch` state.
- Version `965` renders configured Team Mode password hints in team-facing emails and refines the manual payment pending client next-action color. Team email CTAs now include a `Password:` line sourced from the deployed `TEAM_MODE_PASSWORD` Script Property when a Team Mode button is rendered, including order-context action CTAs, account-document Team Mode review CTAs, locked-order resend team CTAs, and client-to-team chat digest buttons. The credential value is not stored in repo docs or source. Manual payment pending client emails now keep the full payment next-action sentence blue instead of turning `Red Threads Portal` red. `Index.html` shows revision `95`. `clasp version` created `965`, the existing stable deployment ID was updated at `@965 - Render team email password hints`, direct Apps Script smoke returned HTTP 200 with `Development revision 95` and omitted stale `Development revision 94`, public `/portal` returned HTTP 200 and still referenced the stable deployment ID, and root smoke checks found no raw Team Mode password marker. The owner email review suite was intentionally not triggered per owner instruction.
- Version `966` refines team email project CTAs and approved account-document team notices. Team Mode project buttons now use `Open Project #... in Team Mode`, client-to-team message digests use `Open Project #... in Team Mode to reply` and place project/client identity plus messages/CTA above order progress, and the immediate client-message team alert uses the same reply CTA. Credit terms approved team and tax exemption approved team emails now put `No action required` at the top, show client name/organization/email details directly below it, and omit the no-action team review CTA. `Index.html` shows revision `96`. `clasp version` created `966`, the existing stable deployment ID was updated at `@966 - Refine team email project CTAs`, direct Apps Script smoke returned HTTP 200 with `Development revision 96` and omitted stale `Development revision 95`, and public `/portal` returned HTTP 200 and still referenced the stable deployment ID. The owner email review suite was intentionally not triggered per owner instruction.
- Version `967` adds client identity details to team email surfaces. Account-document team lifecycle notices, including tax-exemption submitted team review, now include client name, client email, and organization details. Team order-context project details cards now compactly pair project fields with client fields, keeping project name/project total/payment date and adding client name/client email/organization in the same body before history/CTA. Legacy account-document team review HTML also renders the submitter client email. `Index.html` shows revision `97`. `clasp version` created `967`, the existing stable deployment ID was updated at `@967 - Add team email client details`, direct Apps Script smoke returned HTTP 200 with `Development revision 97` and omitted stale `Development revision 96`, and public `/portal` returned HTTP 200 and still referenced the stable deployment ID. The headless owner email review-suite dry run returned `ok:true`; the real suite sent 46 emails, skipped 1 submitted-tax-copy fixture item, reported 0 failures, and reported 30 attachment fallbacks due to the known fixture `artifact_project_mismatch` state.
- Version `968` refines selected client email review surfaces. PO invoice prepared client emails now keep the full purchase-order next-action line blue, including `portal link`. Credit terms approved client emails now render the Details block directly under the green Purchase Order capability callout and above the payment-terms paragraph. PO submitted client emails now use `Purchase Order submitted, production started`; Standard ACH verification client emails now use `Bank verification required before production begins`; Standard ACH pending client emails now use `ACH payment pending, production not started`. `Index.html` shows revision `98`. `clasp version` created `968`, the existing stable deployment ID was updated at `@968 - Refine client email review copy`, direct Apps Script smoke returned HTTP 200 with `Development revision 98` and omitted stale `Development revision 97`, and public `/portal` returned HTTP 200 and still referenced the stable deployment ID. The owner email review suite was intentionally not triggered per owner instruction.
- Version `969` refines the client-to-team chat digest review email and owner review-suite eligibility. `Chat digest client to team` now shows the red `Red Threads Portal Messages` label, removes the old `Client portal message digest` and `A client sent...` header copy, removes the redundant project/client summary object, places the messages table directly below the label, and uses the resolved client full name in the messages-table heading and client message rows instead of generic `Client`. The owner review suite now omits every `recipientClass: client` item through a central class-based omission reason while leaving team and AP review items eligible. `Index.html` shows revision `99`. `clasp version` created `969`, the existing stable deployment ID was updated at `@969 - Refine chat digest review email`, direct Apps Script smoke returned HTTP 200 with `Development revision 99` and omitted stale `Development revision 98`, and public `/portal` returned HTTP 200 and still referenced the stable deployment ID. The owner email review suite was intentionally not triggered per owner instruction.
- Version `970` adds signed Team Mode email auto-unlock links. Team-facing email URL builders now append a scoped 30-day `teamAccess` proof for Team Mode, `teamAction`, and `teamReview` routes; `verifyTeamModeEmailAccess` exchanges valid proofs for the existing Team Mode auth key without putting the Team Mode password in the URL; plain `?t=...&mode=team` links still require the password gate; and the tracked Squarespace wrapper now forwards `teamAction` and `teamAccess`. `Index.html` shows revision `100`. `clasp version` created `970`, the existing stable deployment ID was updated at `@970 - Add Team Mode email auto-unlock links`, direct Apps Script smoke returned HTTP 200 with `Development revision 100`, omitted stale `Development revision 99`, included `teamAccess` and `verifyTeamModeEmailAccess`, and did not expose the signing-secret config key. Public `/portal` returned HTTP 200 and still referenced the stable deployment ID, but the live Squarespace code block did not yet include `teamAccess` or `teamAction`; refresh the live code block from `web/squarespace-portal-code-block.html` before relying on public-wrapper auto-unlock links. The headless owner email review-suite dry run was attempted but blocked because `RT_EMAIL_REVIEW_TRIGGER_SECRET` was not present in the active shell.
- Version `971` refines pending ACH team email action cards. Standard ACH pending team, Standard ACH verification team, and AP ACH pending team emails now render a green `No action required` action-card label, identify the client/order context in the opening line, say the invoice is attached for their order, and show muted `Optional action` production-exception copy without the redundant top-card Team Mode button. The project-details Team Mode link remains available lower in the email. `Index.html` shows revision `101`. `clasp version` created `971`, the existing stable deployment ID was updated at `@971 - Refine pending ACH team email action copy`, direct Apps Script smoke returned HTTP 200 with `Development revision 101` and omitted stale revision `100`, and public `/portal` returned HTTP 200, referenced the stable deployment ID, and included `teamAccess` passthrough. The headless owner email review-suite dry run returned `ok:true`; the real suite sent 23 emails, skipped 24 validated/omitted client-recipient items, reported 0 failures, and reported 18 attachment fallbacks due to the known fixture `artifact_project_mismatch` state.
- Version `972` refines the credit-terms submitted team review email. The account Details container now renders directly below the header, the old two-line review prompt is combined into one clear sentence, and the `Open team review` CTA/password block is centered below that copy. `Index.html` shows revision `102`. `clasp version` created `972`, the existing stable deployment ID was updated at `@972 - Refine credit terms team review email`, direct Apps Script smoke returned HTTP 200 with `Development revision 102` and omitted stale revision `101`, and public `/portal` returned HTTP 200, referenced the stable deployment ID, and included `teamAccess` passthrough. The owner email review suite was intentionally not triggered per owner instruction.
- Version `973` applies the same details-first submitted team-review layout to tax-exemption submissions and updates review-suite omissions. Tax exempt submitted team review emails now place Details directly below the header, use one combined Red Threads review instruction, and center the Team Review CTA/password below the copy. The owner review suite now omits `Tax exempt approved team` and `Credit terms approved team`. `Index.html` shows revision `103`. `clasp version` created `973`, the existing stable deployment ID was updated at `@973 - Refine tax exemption team review email`, direct Apps Script smoke returned HTTP 200 with `Development revision 103` and omitted stale revision `102`, and public `/portal` returned HTTP 200, referenced the stable deployment ID, and included `teamAccess` passthrough. The owner email review suite was intentionally not triggered per owner instruction.
- Version `974` omits the validated AP review fixtures from owner review-suite sends. The owner review suite now skips `AP ACH failed AP`, `AP payment link sent`, `AP ACH pending AP`, and `AP ACH receipt AP`, while preserving the existing client-recipient and approved-team omission policies. `Index.html` shows revision `104`. `clasp version` created `974`, the existing stable deployment ID was updated at `@974 - Omit AP review suite fixtures`, direct Apps Script smoke returned HTTP 200 with `Development revision 104` and omitted stale revision `103`, and public `/portal` returned HTTP 200, referenced the stable deployment ID, and included `teamAccess` passthrough. The owner email review suite was intentionally not triggered per owner instruction.
- Portal DB Sheet ID appears in `Code.js`: `16KrxpEv8s-U5gjLX-DZK25GbrnkeKbjgInngie8Ce_c`.
- `.clasp.json` now points to the live Apps Script project ID verified in Apps Script: `1zv9lbls_bohme0vDA8EZg4G0dyFrsuv3hHO0NOAijSw9imYYNkqMbkKU`.
- `docs/EXPORT_LOG_WIDE_SCHEMA.md` tracks the locked EXPORT_LOG column order.
- `package.json` exposes `npm run validate`, `npm run validate:runtime`, and `npm run validate:binding`.
- Active Squarespace `/portal` iframe wrapper code is tracked at `web/squarespace-portal-code-block.html`; the repo copy now includes centralized route-param forwarding, parent URL replace/push handling, Back/Forward iframe reload from the parent URL, same-window Stripe navigation message handlers, `RT_PORTAL_READY` loader hiding without removing the loader node, `setupResult` passthrough for ACH setup returns, account-dashboard query passthrough for `dashboard`, `accountId`, `accountAccessToken`, Team Mode passthrough for `teamReview`, `teamAction`, and `teamAccess`, and AP `paymentOrigin`, a Stripe-hosted payment/verification navigation allowlist, and a pre-iframe mobile block with a centered ellipsized copy-link display that prevents iframe loading under `900px`.
- Deployed no-reply transport rewire: `sendNotificationEmail_` now enforces Apps Script `MailApp` `noReply: true` delivery for all portal-generated emails and no longer attempts the removed `noreply@redthreads.com` Workspace alias or sets a reply-to address. `noreply@redthreads.com` should remain non-deliverable and not assigned as a user alias.
- Deployed communication suppression policy: `Code.js` now gates owner-disabled informational lifecycle email milestones at queue time and process time without deleting job builders or changing sheet headers. Suppressed milestones are artwork approved/change requested, project ready to order, Team Hold, Project Unlock, Checkout Reset, client flow canceled/reset, standalone production authorized, job/project completion, and AP checkout-started. Payment/legal/action-required email milestones, tax/credit document emails, AP payment-link send, AP ACH submitted/confirmed/failed, standard ACH, payment lifecycle, chat digest, password reset, summary PDF, blank document, and explicit admin resend paths remain outside the suppression policy.
- Deployed communication collision guard: `Code.js` now adds `communication_milestone_superseded` queue-time and process-time policy checks for standard ACH, AP ACH, and non-ACH payment lifecycle email jobs. The guard keeps payment-rail milestones singular, prevents stale AP/standard ACH and PO/manual/card competing queue rows from sending after the current order state changes, preserves disabled-event suppression, leaves existing idempotency keys and queue headers unchanged, and adds current-order validation before `charge.dispute.created` queues ACH action-needed notices.
- Deployed communication CTA hardening: email CTA helpers now build public Red Threads wrapper URLs for Team Mode and document-review routes, default team lifecycle alerts to `/portal?t=<token>&mode=team`, send password-reset links to public `/portal`, and keep AP ACH pending/receipt emails on AP status routes without `payNow=ach` while preserving AP initial-payment and failed/action-needed retry links.
- Deployed communication attachment hardening: lifecycle email attachment policy is now centralized for ACH, AP ACH, and non-ACH payment queue processors; required invoice/receipt/document attachments fail safely with short diagnostic reasons, failed/action-needed payment emails can send without optional invoice attachments, and safe logs now distinguish `attachmentRequired` from `attachmentPresent`. Direct AP payment-link, locked-order/admin resend, PO invoice, blank account-document source, and retained tax/credit approval send paths now block before transport when required documents cannot be loaded.
- Deployed communication content hardening: lifecycle email copy now flows through shared ACH, AP ACH, and non-ACH payment copy models with consistent subjects, opening/status text, next-step copy, attachment notes, and no-reply footer language. Active account-document, chat digest, password reset, invoice utility, AP payment-link, and retained message helpers now use the same automated-notification footer, omit Team Mode passwords from email content, and remove emoji, overlong tax guidance, celebratory approval copy, and legacy "do not reply" language.
- Communication queue hygiene report: read-only inspection of the live `PORTAL_EMAIL_QUEUE` on 2026-06-15 found 66 populated rows: 64 `sent`, 2 intentional `skipped`, 0 `queued`, 0 `processing`, and 0 `failed`; every populated row had one attempt, and the only nonblank skipped reason was `ach_submitted_job_already_paid`. `tools/report-portal-email-queue-hygiene.mjs` now provides a local redacted CSV/TSV queue analyzer for future audits. No Sheet rows were changed.
- Owner email review harness: stable version `919` includes the Team Mode authorized `send_email_review_suite` route for owner-only inbox review. The route can reset active runtime tabs from `FIXTURE_EXPORT`, `FIXTURE_PORTAL_ORDERS`, and `FIXTURE_STRIPE_EVENTS`, verifies fixture headers before copying, clears `PORTAL_EMAIL_QUEUE` data rows for review campaigns, sends active email-family examples with `[EMAIL REVIEW]` subject prefixes and real invoice/receipt/document attachments where available, and returns only redacted label/family/recipient-class/status/attachment-count transport summaries. Client/AP review recipients remain restricted to Red Threads test recipients; the review-suite-only team recipient now uses the owner-approved external test inbox so customer-service mail is not flooded during design review. Normal portal team alerts still route to `DOCUMENT_REVIEW_EMAIL`.
- Portal-rendered invoice/estimate attachment rule: stable version `916` removes the plain server-generated `DocumentApp` invoice fallback from email and email-review attachment paths, rejects known stale fallback artifacts, and requires invoice/estimate review emails to use a freshly captured browser-rendered portal Summary/Invoice PDF. Newly stored portal PDFs receive a Drive description provenance marker, existing unmarked portal-rendered artifacts must match the portal file naming convention, and old `INV-...` fallback files are rejected even if the outbound email attachment name is changed.
- Portal Native email theme: stable version `918` restyles portal-generated HTML email shells to the dark Red Threads portal-native direction with centralized inline email theme tokens, aqua current-state badges/progress, green completed progress, dark cards/panels, crimson used as a controlled brand accent, and the standardized automated no-reply footer. The change preserves existing CTA routes, attachments, queue/idempotency behavior, Stripe/payment behavior, token lookup, pricing authority, and Sheet schemas.
- Email section policy and review-suite recipient update: stable version `919` deploys the lifecycle-section policy that removes Reference/Order Progress/History from client tax-exempt and credit-terms approved/denied/reset decision emails, adds those lifecycle sections to direct client/AP-facing review surfaces when token/order context exists, prevents failed-payment review emails from rendering Payment complete or Production in progress unless production is explicitly authorized, and removes the password-reset Open Portal button/link. Version `919` also changes only the owner email review suite team recipient to the owner-approved external test inbox while preserving normal portal team routing to `hello@redthreads.com`. A deployed browser-triggered review run sent 46 `[EMAIL REVIEW]` emails, skipped the expected submitted-tax-copy fixture item, and reported 0 failures.
- Project-based document references: stable version `920` deploys a centralized project/version document-reference layer for new customer-facing estimate, invoice, receipt, AP, PO, payment, Summary/Invoice, locked-order resend, and email-review references. New refs use `EST-<project>-vNN` / `INV-<project>-vNN`, display as `Estimate <project>-vN`, `Invoice <project>-vN`, or `Receipt for Invoice <project>-vN`, and produce portal-rendered attachment filenames such as `Red Threads - Project <project> - Invoice vNN.pdf`. Existing `invoiceNumber`, `invoicePdfUrl`, `PORTAL_EMAIL_QUEUE`, and `portalStateJson`/`orderDraftJson` carriers are reused without schema/header changes; legacy `INV-YYYYMMDD-...` values remain valid internal/historical fallbacks.
- Password reset return-link flow: stable version `925` keeps the existing reset-code-required flow and fixes public return routing through the currently live Squarespace wrapper by sending new reset emails with a wrapper-compatible `accountDocAction=reset_<token>` bridge. Apps Script decodes that bridge into the reset-code/new-password route, while direct Apps Script and future refreshed wrappers still support `reset=1&resetToken=...`. Direct Apps Script bridge smoke and public wrapper bridge smoke returned HTTP 200 with no targeted secret markers.
- Portal Native dashboard snapshot email layout: stable version `922` deploys the updated shared lifecycle email layout. Order-context lifecycle emails now use the action-first Dashboard Snapshot structure with a red `Red Threads Portal` eyebrow, no top aqua strip, no header status badge, specific update headings, top CTA placement, `Current Order Stage` progress with owner-approved stage labels, completed-stage date labels when existing reliable dates are available, and a consolidated dynamic `Project #... Details` card with a small red project portal CTA. The change preserves existing CTA routes, portal-rendered attachments, queue/idempotency behavior, Stripe/payment behavior, token lookup, pricing authority, no-reply transport, and Sheet schemas. Direct Apps Script and public wrapper route smoke returned HTTP 200 with no targeted secret markers. The owner review-suite trigger was attempted from the supplied Team Mode route but blocked by browser automation limits: the public wrapper iframe loaded the stable Apps Script target, while direct iframe navigation showed Google's Apps Script interstitial, so the suite was not triggered from Codex in this ship.
- Headless owner email review trigger: stable version `923` deploys a protected `send_email_review_suite_headless` web action and local `npm run email-review:suite` command so Codex can trigger the owner inbox-review suite without browser/iframe execution. The headless path uses Script Property `EMAIL_REVIEW_TRIGGER_SECRET`, reuses the latest validated portal-rendered invoice artifact from fixture/order rows, supports dry-run and recipient overrides, preserves the existing browser/Team Mode review path, and returns only redacted suite summaries. Deployed smoke confirmed the direct Apps Script route and public wrapper return HTTP 200 with no targeted secret markers, and a fake-secret POST safely returns `email_review_trigger_secret_not_configured`; the one-time Script Property and local `RT_EMAIL_REVIEW_TRIGGER_SECRET` must be set before the command can send emails.
- Compact email progress rail: stable version `924` replaces the large lifecycle progress cards with the compact Layout 3 email progress rail. Shared order-context lifecycle emails now render `CURRENT ORDER STAGE` with email-safe table cells, 7px horizontal stage bars, green completed bars/status dates, aqua current bars/status, muted future bars/status, and the same owner-approved stage labels. The release also ships the document-aware email heading patch so project notifications keep full labels such as `Invoice 2011-v1 — ACH payment pending` instead of collapsing to version-only headings. Direct Apps Script and public wrapper route smoke returned HTTP 200 with no targeted secret markers, and the headless owner review suite sent 46 review emails, skipped the expected submitted-tax-copy fixture item, and reported 0 failures.
- Password reset return-route compatibility bridge: stable version `925` updates new password-reset email buttons to use a public `/portal` URL parameter already forwarded by the current live Squarespace wrapper, then decodes that bridge inside Apps Script and the auth shell. The reset token remains non-auth and cannot reset a password without the emailed code and new password. Existing direct `reset=1&resetToken=...` support remains for internal testing and any future wrapper refresh.
- Order-context email structure update: stable version `926` makes shared order-context emails project-first and action-first. Lifecycle/order emails now render a `Project #...` header with a document/action subheading, move immediate client/team action copy into a dark action card above progress, rename the progress section to `CURRENT ORDER PROGRESS`, show `Add Quantity / Sizes`, use `Completed` when no reliable completed-stage date exists, show `Current action` for the active step, remove redundant `Project #` and `Payment method` rows from Project Details, add method-aware payment history labels, and add production completion/turnaround timing only from reliable persisted order/job data. Direct Apps Script and public wrapper routes returned HTTP 200, and the headless owner review suite sent 46 emails, skipped the expected submitted-tax-copy fixture item, and reported 0 failures.
- Order-context email microcopy update: stable version `927` refines the shared order-context lifecycle shell. Portal Native email eyebrows now render `Red Threads Portal - Notification`; completed progress stages show `Completed M/D` when a reliable date exists and `Completed` otherwise; production-current stages render `In Production` with `Started M/D` when available; action cards resolve to `Action required`, `No action required`, or `Current action`; no-action copy no longer repeats a redundant `Next action` sentence; estimate/invoice/receipt attachment language is promoted into the action card; top `View Red Threads receipt` CTAs are suppressed; and production timing is promoted into the action card when existing order data proves production is active. Direct Apps Script and public wrapper routes returned HTTP 200, and the headless owner review suite sent 46 emails, skipped the expected submitted-tax-copy fixture item, and reported 0 failures.
- Email review attachment selection: stable version `929` changes the owner email review suite to resolve invoice/receipt attachments from each review order's own stored portal-rendered artifact first instead of writing one global artifact pointer across all fixture/order rows. The suite now validates the chosen artifact through the existing portal-rendered attachment guard, performs safe semantic matching against project metadata when available, and only uses the latest valid portal-rendered fallback when the order-specific artifact is missing or mismatched. Redacted suite output now reports attachment matched/fallback counts and per-item attachment reasons without exposing tokens, recipients, Drive URLs, row JSON, or state JSON.
- Portal email action styling: stable version `930` keeps order-context email headers in the normal white Portal Native heading color even when the update text contains `Action required`, while the action-card label itself uses the existing bright aqua emphasis for client-facing required actions. The change preserves CTA routes, attachment policy, queue/idempotency behavior, recipient routing, no-reply transport, token lookup, pricing authority, Stripe behavior, Sheet schemas, and the existing email review suite behavior.
- Portal message/account-document email refinement: stable version `931` updates the client portal-message digest to use the Portal Native shell with the existing message panel and reply CTA above the current order progress/project details sections, changes the client digest CTA to `Reply to this message in your portal`, ships the tax-exemption blank-document Portal Native upload CTA/deeplink parity, preserves the reset/account-document route-loader compatibility work, and keeps estimate-copy subject/body copy aligned to `Project <number> Estimate Copy`. The change preserves queue semantics, attachment policy, no-reply transport, recipient routing, token lookup, pricing authority, Stripe behavior, Sheet schemas, and the existing review-suite trigger behavior.
- ACH/AP payment email copy refinement: stable version `932` updates shared ACH verification copy, removes `Action required` / `Action needed` labels from Portal Native email headers while keeping body action-card labels, renders no-action body labels in completed-stage green, relabels pending/current/future payment progress as `Complete Payment`, moves AP payment-link purchaser notes above the AP greeting, moves the AP payment-link CTA under the payment instruction copy, and makes AP ACH receipt emails render as green `No action required` with concise production-authorization and invoice-attachment copy. The change preserves queue semantics, attachment policy, no-reply transport, recipient routing, token lookup, pricing authority, Stripe behavior, Sheet schemas, and deployment ID.
- Portal runtime refinements: stable version `933` ships the account-document lifecycle copy isolation, account-document dashboard deep-link boot optimization, tax-exemption viewer robustness, password-reset CTA centering, estimate-copy project-number fallback hardening, and locked-order resend paid-state guard. Account-document lifecycle emails no longer inherit order/project `nextStepText`, credit-terms approval uses account-level payment-terms wording, submitted tax exemption copy references tax-exemption status instead of project status, and the visible revision badge is `65`. Direct Apps Script and public wrapper smoke returned HTTP 200, and the headless owner email review suite sent 46 emails, skipped the expected submitted-tax-copy fixture item, and reported 0 failures.
- Account-document upload deeplink boot repair: stable version `934` fixes credit-terms and tax-exemption upload deeplinks that could fall back to the Portal Login view after the route loader ran. `Code.js` now preserves `t`/`token` in server-rendered request-route metadata, and `Index.html` captures the dashboard route token before auth-shell runtime state clears `CURRENT_TOKEN`, then uses that captured token for lightweight account-document dashboard boot. Direct Apps Script and public wrapper smokes for both `credit_terms` and `tax_exempt` resubmit links reached the intended upload workspaces without login fallback. The tax-exemption page-2 preview can still show a non-blocking source-asset warning, but upload remains available. No Sheet schema, token lookup, pricing, Stripe, queue, no-reply, or deployment-ID behavior changed.
- Project-details email label refinement: stable version `935` updates shared order-context project details containers to show `Project name` without the leading project number when the name starts with that number, changes amount rows to `Project total`, keeps the project number in the container header, removes the locked-order resend action-card `View your order summary` button, keeps `CURRENT ORDER PROGRESS` white, and increments the visible revision badge to `66`. No Sheet schema, token lookup, pricing, Stripe, queue, no-reply, attachment, or deployment-ID behavior changed.
- Portal load transition optimization: stable version `937` adds redacted load timing for portal entry, dashboard, token/project RPC, and client transitions; avoids duplicate token-project VM work by passing existing request context; changes public-traffic email queue nudging so it does not replace an existing processor trigger; adds account-dashboard project caching, full-project dashboard prefetch, safe cached project-to-dashboard returns with mutation freshness guards, and lazy tax-form manifest loading; increments the visible revision badge to `67`. No Sheet schema/header, token lookup semantics, pricing authority, Stripe config/live mode, Script Properties, or deployment ID changed. The tracked wrapper also hides loading on `RT_PORTAL_READY`, but public Squarespace wrapper changes require refreshing the Squarespace code block from the repo copy.
- Password reset prefill and ACH card cleanup: stable version `938` resolves valid password-reset return tokens during initial Apps Script render, injects the reset email into safe route metadata, locks the reset-code email field read-only, sends the locked reset token back with linked reset submissions for email/token agreement, removes the intermediate `Opening password reset. One moment...` state for resolved reset links, and simplifies the account-dashboard ACH payment accounts card to only `ACH payment accounts` / `Manage accounts`; the visible revision badge is now `68`. No Sheet schema/header, token lookup semantics, pricing authority, Stripe config/live mode, Script Properties, email queue behavior, or deployment ID changed.
- Account-document dashboard hydration and portal-message email polish: stable version `939` starts full dashboard project hydration in the background as soon as credit-terms or tax-exemption upload deeplinks complete lightweight account-document boot, coalesces duplicate dashboard-home requests, shows `Your projects are loading into view...` under the Project Details / Order Progress headers when an upload modal closes before rows render, and keeps mutation freshness guards for account-document submissions/uploads. Client-facing portal-message digest emails now remove the redundant `Messages` table label/object, render the message directly under the intro copy, and color Red Threads team-authored message metadata/content in Red Threads red. The visible revision badge is now `69`. Direct Apps Script and public wrapper smoke returned HTTP 200 with no targeted secret markers, and the headless owner email review suite sent 46 emails, skipped the expected submitted-tax-copy fixture item, and reported 0 failures.
- Dashboard project name wrapping and review-suite omission: stable version `940` fixes dashboard project list layout by locking thumbnail/progress/action columns and allowing long project names to wrap to two lines inside the project-name row without pushing the Order Progress rail or Open Project button. The release also preserves the ACH pending next-action copy update and omits the validated password-reset fixture from the owner email review suite. The visible revision badge is now `70`. Direct Apps Script and public wrapper smoke returned HTTP 200 with no targeted secret markers, and the headless owner email review suite sent 45 emails, skipped submitted-tax-copy plus password-reset, and reported 0 failures.
- Credit terms source email refinement: stable version `941` updates the blank credit-terms source email instructions to tell clients to complete/sign the attached PDF, open the portal dashboard to the Credit Terms section, upload the document in the portal, and wait for team review. The owner email review suite now omits the validated `Blank credit terms source client` fixture in addition to password reset and submitted-tax-copy. The visible revision badge is now `71`. Direct Apps Script and public wrapper smoke returned HTTP 200 with no targeted secret markers, and the headless owner email review suite sent 44 emails, skipped 3, and reported 0 failures.
- Dashboard project row geometry repair: stable version `942` replaces the flexible dashboard projects table sizing with fixed row geometry: 1080px table width, explicit thumbnail/details/progress/action columns, a 410px fixed progress rail, and a two-line `Name:` value cap. Long project names now wrap inside the project-name value instead of changing the Order Progress rail or Open Project column. The visible revision badge is now `72`. Direct Apps Script and public wrapper smoke returned HTTP 200 with no targeted secret markers; browser visual QA confirmed the public dashboard renders revision `72`, fixed-width progress rails, and full Open Project buttons.
- Tax source email and review-suite omission refinement: stable version `943` renders the blank tax document source instruction paragraph in Red Threads red when an upload CTA is available, changes the CTA/plain-text label to `Upload Complete Tax Exemption Form`, and omits the validated `Chat digest team to client` and `Blank tax document source client` fixtures from the owner email review suite. The visible revision badge is now `73`. Direct Apps Script and public wrapper smoke returned HTTP 200 with no targeted secret markers; the headless owner email review suite sent 42 emails, skipped 5, and reported 0 failures.
- Estimate-copy sender review panel: stable version `944` adds the Portal Native aqua note panel used by account-document next-step notices to estimate-copy emails, above the project-name row, with copy that says the Red Threads Project Estimate was sent by the client full name for review. Live sends pass the current client name from the portal payload; the owner review fixture resolves the Project 1900 client name from fixture data. The visible revision badge is now `74`. Direct Apps Script and public wrapper smoke returned HTTP 200 with no targeted secret markers; the headless owner email review suite sent 42 emails, skipped 5, and reported 0 failures.
- Credit terms reset email refinement: stable version `945` renders client credit-terms reset emails with the next-step panel first under the header, the account-dashboard status copy next, and a centered `Complete New Credit Terms Application` CTA below it. The owner email review suite now omits the validated `Explicit locked-order resend client` fixture. The visible revision badge is now `75`. Direct Apps Script and public wrapper smoke returned HTTP 200 with no targeted secret markers. The owner email review suite was intentionally not triggered for this ship per owner instruction.
- PO and credit-terms approved email refinement: stable version `946` updates the PO invoice prepared client next-action copy to tell clients to forward the invoice to Purchasing or Accounts Payable, highlights `portal link below` in Red Threads red inside the action card, and rearranges the credit-terms approved client email so `No action is required.` appears first, followed by approved account copy, details, and a centered `Open Red Threads Portal Account Dashboard` button. The visible revision badge is now `76`. Direct Apps Script and public wrapper smoke returned HTTP 200 with no targeted secret markers; the headless owner email review suite sent 41 emails, skipped 6, reported 0 failures, and reported 29 attachment fallbacks due to the known fixture `artifact_project_mismatch` state.
- Account email object order refinement: stable version `947` expands the approved account-email layout to tax-exemption approved client emails, applies the next-step-first account layout to credit-terms/tax-exemption denied and reset client emails, keeps team account-document alerts unchanged, and centers account CTAs after account details. The visible revision badge is now `77`. Direct Apps Script and public wrapper smoke returned HTTP 200 with no targeted secret markers; the headless owner email review suite sent 41 emails, skipped 6, reported 0 failures, and reported 29 attachment fallbacks due to the known fixture `artifact_project_mismatch` state.
- PO and credit-terms review email refinement: stable version `948` removes the duplicate PO invoice prepared client attachment sentence, keeps only `portal link` highlighted in Red Threads red inside the PO next-action copy, changes credit-terms denied client emails to the requested not-approved heading with no status/next-step panel, changes their CTA to `Resubmit Credit Terms Document`, and omits the validated estimate-copy and credit-terms reset fixtures from future owner review-suite sends. The visible revision badge is now `78`. Direct Apps Script and public wrapper smoke returned HTTP 200 with no targeted secret markers. The owner email review suite was intentionally not triggered per owner instruction.
- Account-document review email refinement: stable version `949` preserves intentionally blank account-document status/next-step copy so removed objects do not fall back into rendered emails, removes the credit-terms approved client no-action object, keeps credit-terms denied client emails without the generic status line, removes the tax-exemption reset client next-step object, changes the tax-exemption reset client status copy to the Sales Tax Exemption resubmission sentence, and changes that CTA to `Complete Sales Tax Exemption Form`. The visible revision badge is now `79`. Direct Apps Script and public wrapper smoke returned HTTP 200 with no targeted secret markers. The owner email review suite was intentionally not triggered per owner instruction.
- Tax-exemption denied email refinement: stable version `950` makes tax-exemption denied client emails mirror the credit-terms denied client structure: tax-specific not-approved heading/intro, no status paragraph, no next-step object, account details before the centered CTA, and `Resubmit Sales Tax Exemption Form` as the CTA. Team tax-exemption denied alerts keep their existing review workflow copy. The visible revision badge is now `80`. Direct Apps Script and public wrapper smoke returned HTTP 200 with no targeted secret markers. The owner email review suite was intentionally not triggered per owner instruction.
- Tax-approved and PO payment email refinement: stable version `951` changes tax-exemption approved client emails so the no-action object reads `You can now toggle sales tax on and off your orders.` and removes the old approved-tax-status paragraph. PO submitted client emails now say the order is authorized for production, use the invoice/receipt payment-required attachment sentence, and tell clients to return to the portal/project to pay within approved terms with late-fee warning. PO payment received client emails now combine the invoice/receipt attachment and portal-update copy into one line. The visible revision badge is now `81`. Direct Apps Script and public wrapper smoke returned HTTP 200 with no targeted secret markers. The owner email review suite was intentionally not triggered per owner instruction.
- Manual payment receipt email hardening: stable version `952` prevents non-ACH payment receipt milestones from inheriting stale payment-required next actions, updates the manual payment received client email to combine payment receipt and production authorization in the opening sentence, and changes that client attachment sentence to `The invoice for your order is attached to this email.` The visible revision badge is now `82`. Direct Apps Script and public wrapper smoke returned HTTP 200 with no targeted secret markers. The headless owner email review-suite dry run was blocked locally because `RT_EMAIL_REVIEW_TRIGGER_SECRET` was unset; no real review emails were sent.
- Manual payment pending email refinement: stable version `953` changes manual payment pending client emails to use the `Order Placed, Payment Required to Begin Production` project header, invoice/receipt attachment copy, the new payment next-action copy with Red Threads Portal highlighted in red, and the check/cash payment instruction block with Red Threads LLC payee and exact amount. The visible revision badge is now `83`. Direct Apps Script and public wrapper smoke returned HTTP 200 with no targeted secret markers. The headless owner email review-suite dry run was blocked locally because `RT_EMAIL_REVIEW_TRIGGER_SECRET` was unset, so the real suite was not sent.
- Card failed email copy and timing guard: stable version `954` changes card-failed client emails to use the `Credit card payment issue` project header, credit-card-specific retry copy, and `Open your Red Threads project/portal below and retry payment.` next-action text. Blocked-payment emails now suppress production timing/history unless production is explicitly authorized. The visible revision badge is now `84`. Direct Apps Script and public wrapper smoke returned HTTP 200 with no targeted secret markers. The headless owner email review-suite dry run returned `ok:true`; the real suite sent 39 emails, skipped 8, reported 0 failures, and reported 29 attachment fallbacks due to the known fixture `artifact_project_mismatch` state.
- Email review omission and PO receipt copy cleanup: stable version `955` omits the validated credit-terms approved/denied, tax-exemption approved/denied/reset, PO submitted/payment received, manual payment received/pending, and card failed client fixtures from future owner review-suite sends. Tax-exemption approved client emails now render the sales-tax toggle notice with the green no-action styling, and PO payment received client emails now say `Your invoice/receipt is attached to this email and available in your portal.` The visible revision badge is now `85`. Direct Apps Script cache-busted `/exec` smoke returned HTTP 200, contained `Development revision 85`, and omitted stale `Development revision 84`; public wrapper smoke returned HTTP 200 and still referenced the stable deployment ID. The owner email review suite was intentionally not triggered per owner instruction.

## ACH V1 Live State

- ACH V1 infrastructure is merged to `main` and deployed to the existing stable Apps Script deployment.
- Stable Apps Script deployment `AKfycbz9qDgp65f5S3RWhSxGftioMXKKU9O1N0mpHh3waoKY2YyvE72F-cJk-0XYr5YXg4bw` is currently version `955`, `Omit validated email review fixtures`.
- Version `930` fixes the shared Portal Native email shell color targeting for action-required project/order messages. `Code.js` no longer turns the main project/update header aqua when the heading contains `Action required`; headers remain the normal white/muted treatment, and the shared action card continues to render `Action required` in the bright portal aqua. Pre-deploy validation passed, `clasp version` created `930`, and the existing stable deployment ID was updated without creating a new deployment. Direct Apps Script `/exec` and public `/portal` returned HTTP 200 with no targeted secret markers. The headless owner review suite dry-run returned `ok:true`; the real review run sent 46 emails, skipped the expected submitted-tax-copy fixture item, reported 0 failures, and reported 30 attachment fallbacks due to the current fixture artifact project mismatch state.
- Version `929` fixes the headless owner email review suite attachment-selection behavior. `Code.js` no longer rewrites every `PORTAL_ORDERS` / `FIXTURE_PORTAL_ORDERS` row to point at one global portal-rendered artifact before sending. Review emails now attempt each order's own `invoicePdfUrl` / `invoiceFileId` first, validate the artifact with the existing portal-rendered attachment guard, perform safe project-level semantic matching, then use the latest valid portal-rendered fallback only when the order artifact is missing or mismatched. Redacted result rows now include `attachmentMatched`, `attachmentFallback`, and `attachmentReason`, and `tools/send-email-review-suite.mjs` prints matched/fallback counts. Pre-deploy validation passed, `clasp version` created `929`, and the existing stable deployment ID was updated without creating a new deployment. Direct Apps Script `/exec` and public `/portal` returned HTTP 200 with no targeted secret markers. The headless owner review suite dry-run returned `ok:true`; the real review run sent 46 emails, skipped the expected submitted-tax-copy fixture item, reported 0 failures, and reported 30 attachment fallbacks due to `artifact_project_mismatch`, proving the current fixture order artifacts still need per-project portal-rendered refresh before inbox attachment visuals can match linked live project invoices.
- Version `925` fixes password reset return routing through the currently live public wrapper. New reset emails now build public Red Threads `/portal` links with a wrapper-compatible reset bridge value instead of relying only on `resetToken`, which the live wrapper was dropping. Server request-route metadata and the client auth shell both decode the bridge into the reset-code/new-password view; the token still only prefills/routes the form and does not replace the emailed reset code. Pre-deploy validation passed, `clasp version` created `925`, and the existing stable deployment ID was updated without creating a new deployment. Direct Apps Script bridge smoke, legacy direct reset-param smoke, and public-wrapper bridge smoke returned HTTP 200 with no targeted `client_secret`, `clientSecret`, or `hosted_verification_url` markers.
- Version `924` deploys the compact Layout 3 progress rail for shared order-context lifecycle emails. `Code.js` now renders `CURRENT ORDER STAGE` as a compact table-based progress rail with 7px bars above each stage label, green completed status/date treatment, aqua current treatment, muted future treatment, and the owner-approved stage labels. Completed stages continue to show existing reliable dates in `M/D/YY` when available and `Done` only when no reliable date exists. The release also includes document-aware email headings for lifecycle and direct project emails, preserving full labels such as `Invoice 2011-v1` or `Estimate 2010-v1` in rendered email headers. Pre-deploy validation passed, `clasp version` created `924`, and the existing stable deployment ID was updated without creating a new deployment. Direct Apps Script `/exec` and public `/portal` returned HTTP 200 with no targeted `client_secret`, `clientSecret`, or `hosted_verification_url` markers. The headless owner review suite dry-run returned `ok:true`, then the real review run sent 46 emails, skipped the expected submitted-tax-copy fixture item, and reported 0 failures.
- Version `923` deploys the protected headless owner email review trigger. `Code.js` now accepts `send_email_review_suite_headless` only when `triggerSecret` matches Script Property `EMAIL_REVIEW_TRIGGER_SECRET`, uses `LockService` to prevent overlapping suite sends, supports `dryRun`, and reuses the latest validated portal-rendered invoice artifact from `FIXTURE_PORTAL_ORDERS` or `PORTAL_ORDERS` instead of requiring browser PDF capture. `tools/send-email-review-suite.mjs` and `npm run email-review:suite` provide the local trigger command with redacted output and local `.env` support. Pre-deploy validation passed, `clasp version` created `923`, and the existing stable deployment ID was updated without creating a new deployment. Direct Apps Script `/exec` and public `/portal` returned HTTP 200 with no targeted secret markers. A fake-secret POST returned `email_review_trigger_secret_not_configured`, confirming the deployed action is present and safely blocked until the one-time Script Property/local secret setup is completed.
- Version `922` deploys the Portal Native Dashboard Snapshot email layout. Shared lifecycle/order-context email rendering now moves the primary CTA near the top, renames the progress section to `Current Order Stage`, uses `Add Quantity Sizes`, `Approve Artwork`, `Place Order`, `Make Payment`, and `Production Begins` stage labels, displays completed-stage dates in `M/D/YY` when reliable existing dates are available, and consolidates metadata/history/attachment notes into a dynamic `Project #... Details` card with a small red project CTA. The header no longer renders the aqua top strip or status badge, and the `Red Threads Portal` eyebrow is Red Threads red. Pre-deploy validation passed, `clasp version` created `922`, and the existing stable deployment ID was updated without creating a new deployment. Direct Apps Script `/exec` and public `/portal` returned HTTP 200; direct source smoke showed revision `64` and no targeted `client_secret`, `clientSecret`, or `hosted_verification_url` markers. The owner email review suite was not triggered from Codex because the automation surface could not execute inside the cross-origin Apps Script iframe and direct iframe navigation returned Google's Apps Script interstitial.
- Version `921` deploys the password reset return-link server/client runtime. `authSendResetCode` now creates a short-lived non-auth CacheService reset-return token, emails a public Red Threads reset button, and preserves the existing code-required password reset flow. `Index.html` can resolve `reset=1&resetToken=...` to the reset-code/new-password view and prefill the email when the token is valid; invalid or expired tokens show safe request-new-code copy. Pre-deploy validation passed, `clasp version` created `921`, and the existing stable deployment ID was updated without creating a new deployment. Direct Apps Script `/exec?reset=1&resetToken=<synthetic>` returned HTTP 200 and contained the reset-return client code with no `client_secret`, `clientSecret`, or `hosted_verification_url` markers. Public `/portal?reset=1&resetToken=<synthetic>` returned HTTP 200 and still targets the stable deployment ID, but the live Squarespace wrapper source did not yet include `resetToken`, so the tracked wrapper code must be published to Squarespace before branded reset links fully work.
- Version `920` deploys project-based customer-facing estimate/invoice/receipt references and portal-rendered attachment filenames. New estimate/invoice refs use `EST-<project>-vNN` and `INV-<project>-vNN`; email subjects, lifecycle reference blocks, AP payment-link copy, PO invoice/resend copy, Summary/Invoice utility emails, rendered PDF labels, and owner email-review examples resolve to labels such as `Estimate 2010-v1`, `Invoice 2010-v1`, and `Receipt for Invoice 2010-v1`. Existing Sheet headers, queue payload schemas, `snapshotJson`, pricing authority, token lookup, no-reply transport, Stripe/payment behavior, and portal-rendered attachment policy were preserved. Pre-deploy validation passed, `clasp version` created `920`, and the existing stable deployment ID was updated without creating a new deployment. Direct Apps Script `/exec` and public `/portal` returned HTTP 200; the public wrapper still targets the stable deployment ID, and targeted rendered-response checks found no `client_secret`, `clientSecret`, or `hosted_verification_url` markers.
- Version `919` deploys the email-section and payment-blocked lifecycle repair that had previously been blocked by Google reauthentication, plus the review-suite-only team recipient update. `Code.js` now has a shared lifecycle section policy that removes Reference/Order Progress/History from client tax-exempt and credit-terms approved/denied/reset decision emails, adds those sections to direct client/AP-facing review surfaces such as AP payment link, client chat digest, purchase-order invoice, locked-order resend/order confirmation, and Summary/Invoice utility emails when token/order context exists, guards ACH/AP/card failed-payment progress so Payment is current/action-needed and Production is future unless explicitly authorized, and removes the password-reset Open Portal button/link. `Index.html` passes token context with Summary/Invoice PDF email sends and defaults owner-review team examples to the owner-approved external test inbox. Pre-deploy validation passed, `clasp version` created `919`, and the existing stable deployment ID was updated without creating a new deployment. A deployed browser-triggered review run from unlocked Team Mode sent 46 `[EMAIL REVIEW]` emails, skipped the expected submitted-tax-copy fixture item, and reported 0 failures.
- Version `918` deploys the Portal Native email theme to portal-generated HTML emails. Shared lifecycle emails and direct utility/review emails now render through dark portal-native surfaces with centralized email style tokens, specific update headings, aqua current-state badges, green completed progress, dark reference/history/attachment/CTA panels, and the current automated no-reply footer. Pre-deploy validation passed, `clasp push --force` reported the runtime was already up to date, `clasp version` created `918`, and the existing stable deployment ID was updated without creating a new deployment. Direct Apps Script `/exec` and public `/portal` returned HTTP 200; the public wrapper still targets the stable deployment ID, and targeted rendered-response checks found no `client_secret`, `clientSecret`, or `hosted_verification_url` markers. The owner email review suite was reached through an unlocked Team Mode browser session, but the review blast did not complete during this ship because the portal-rendered PDF capture step returned the existing Summary/Invoice render error before sending.
- Version `916` rejects stale non-portal invoice artifacts and repairs the owner email review suite artifact source. Invoice-family attachment policies now validate portal-rendered provenance for required and optional invoice attachments; optional action-needed paths may omit a bad/missing attachment but cannot send the old plain invoice PDF. `storePortalPdfBlobInInvoiceFolder_()` marks browser-rendered portal PDFs with `redthreads_portal_rendered_summary_invoice_v1`, and the review suite now requires a browser-captured portal PDF, stores it, refreshes both `PORTAL_ORDERS` and `FIXTURE_PORTAL_ORDERS` invoice artifact pointers for disposable review rows, and then sends the inbox-review examples. Pre-deploy validation passed, `clasp version` created `916`, and the existing stable deployment ID was updated without creating a new deployment. A deployed browser-triggered review run sent 46 `[EMAIL REVIEW]` emails, skipped only the expected submitted-tax-copy fixture item, had 0 failures, refreshed 7 active order rows plus 7 fixture order rows with the marked portal artifact, and a post-run artifact audit checked 8 invoice families with 0 rejected artifacts.
- Version `915` requires portal-rendered invoice/estimate artifacts for email attachments. Standard card/ACH checkout, manual/check/cash order placement, and AP payment-link preparation now capture/store the portal-rendered Summary/Invoice PDF before the order advances; ACH/AP/payment lifecycle, purchase-order invoice, locked-order/admin resend, and email-review attachment paths reuse stored portal-rendered artifacts and no longer call the plain server-side invoice renderer. Required invoice/receipt sends fail safely with `required_portal_rendered_invoice_missing` when the portal artifact is unavailable, while action-needed paths keep optional attachment behavior. Pre-deploy validation passed, `clasp push --force` pushed the tracked runtime files, `clasp version` created `915`, and `clasp deploy` updated the existing stable deployment ID without creating a new deployment. Direct Apps Script `/exec` and public `/portal` returned HTTP 200, the direct runtime showed `Development revision 63`, the public wrapper still targets the stable deployment ID, and targeted rendered-response checks found no `client_secret`, `clientSecret`, `hosted_verification_url`, `server_fallback`, or plain-invoice marker.
- Version `914` hardens the owner email review fixture reset by verifying source and target tab headers before copying fixture data into active runtime tabs. The change preserves the @913 review harness behavior and adds a schema/header guard for future review campaigns; no new deployment ID was created.
- Version `913` adds an owner-only email review harness. Validation passed before deployment, the existing stable deployment ID was updated without creating a new deployment, and one deployed review run reset from fixture tabs, cleared review queue rows, and sent 46 `[EMAIL REVIEW]` messages across standard ACH, AP payment/AP ACH, card, manual/check/cash, PO, tax/credit lifecycle, chat digest, purchase-order invoice, locked-order resend, summary PDF, blank document source, and password-reset families. All sent messages reported `transport=mailapp_noreply` and `noReply=true`; one submitted tax-form copy review item was intentionally skipped because the current fixture has no submitted artifact. No raw tokens, queue payload JSON, recipient JSON, Drive URLs, or private state JSON were logged.
- Version `912` exposes existing UI server actions through `doPost` for smoke parity: `queue_purchase_order_invoice_email` maps to `queuePurchaseOrderInvoiceEmail`, and `auth_send_reset_code` maps to `authSendResetCode`. Post-deploy smoke used a disposable project through normal app actions to send the PO invoice email, submit a PO, and mark PO payment received; the new purchase-order invoice row, PO-submitted client/team pair, and PO-payment-received client/team pair all completed `sent` with one attempt and no visible failure reason. Password-reset smoke returned `ok:true`; source inspection confirms that reset emails use the shared `sendNotificationEmail_` `MailApp` transport with `noReply:true` and a public portal CTA. Queue health after this pass showed 89 populated job rows, all `sent`, all attempt count `1`, with no visible `lastError`, `queued`, `processing`, or `failed` rows.
- Version `911` fixes a card-payment communication smoke defect in locked-order checkout: `createLockedOrderPaymentCheckout_()` now persists the selected payment method on the current order when creating the Stripe Checkout session. Post-deploy smoke confirmed card failure created exactly the expected `payment_lifecycle_email` client/team action-needed pair, card success created exactly the expected client/team receipt pair, all four rows completed `sent` with one attempt and no visible failure reason, and no standalone `production_authorized` email row was created. Queue health after this pass showed 84 populated rows, all `sent`, all attempt count `1`, with no visible `lastError`, `queued`, `processing`, or `failed` rows.
- Version `910` is the current full-smoke preparation deployment marker for the completed six-phase communication tranche. `clasp push --force` reported the Apps Script runtime was already up to date, `clasp version` created `910`, and `clasp deploy` updated the existing stable deployment ID. Direct Apps Script `/exec` and public `/portal` returned HTTP 200, the direct runtime showed `Development revision 63`, stale `Development revision 62` was absent, targeted rendered-response checks found no `client_secret`, `clientSecret`, or `hosted_verification_url` markers, and the public wrapper still targets the stable deployment ID. A read-only non-payload `PORTAL_EMAIL_QUEUE` baseline showed 68 populated rows, all `sent`, all attempt count `1`, with no visible failure reason, queued rows, processing rows, or failed rows.
- Version `909` is the full-smoke deployment marker for the route-parity patch needed by the communication fixture smoke loop. `Code.js` now exposes existing credit-terms team review/approval, PO submit, and chat append server actions through the existing `doPost` action dispatcher for automated smoke calls; Team-authored chat writes require a valid Team Mode auth key before `appendChatMessage` runs, while client-authored chat remains token-based. `clasp push --force` reported the Apps Script runtime was already up to date, `clasp version` created `909`, and `clasp deploy` updated the existing stable deployment ID. Direct Apps Script `/exec` and public `/portal` returned HTTP 200, the direct runtime showed `Development revision 63`, stale `Development revision 62` was absent, targeted rendered-response checks found no `client_secret`, `clientSecret`, or `hosted_verification_url` markers, and the public wrapper still targets the stable deployment ID.
- Version `906` is a full-smoke preparation marker for the current deployed communication tranche and active-order resolver smoke fixes. `clasp push --force` reported the Apps Script runtime was already up to date, `clasp version` created `906`, and `clasp deploy` updated the existing stable deployment ID. Direct Apps Script `/exec` and public `/portal` returned HTTP 200, the direct runtime showed `Development revision 61`, targeted rendered-response checks found no `client_secret`, `clientSecret`, or `hosted_verification_url` markers, and the public wrapper still targets the stable deployment ID. A read-only queue baseline using non-payload columns showed 59 populated `PORTAL_EMAIL_QUEUE` rows, all `sent`, all attempt count `1`, with no visible failure reason.
- Version `905` extends the current-order resolver fix from `904` to the remaining active-order read paths found during fixture smoke prep: portal finalized-save checks, Team Mode/VM project payload construction, and account-document reset workflows now prefer `EXPORT_LOG.activeOrderId` through `getCurrentPortalOrderForRow_()` when an export row is available. Pre-commit validation passed, direct Apps Script `/exec` and public `/portal` returned HTTP 200, the direct runtime showed `Development revision 61`, targeted rendered-response checks found no `client_secret`, `clientSecret`, or `hosted_verification_url` markers, and the public wrapper still targets the stable deployment ID. A read-only queue baseline after the tax-exempt reset smoke showed 55 populated `PORTAL_EMAIL_QUEUE` rows, all `sent`, all attempt count `1`, with no visible failure reason; the tax-exempt reset client/team rows both completed `sent`.
- Version `904` fixes current-order resolution discovered during fixture smoke: admin/order lifecycle paths now prefer the `EXPORT_LOG.activeOrderId` current-order pointer when an export row is available, and generic latest-order fallback sorting now uses `lastUpdatedAt`/`createdAt` before revision number so older revisions cannot outrank newly placed orders for the same token. Post-904 smoke placed a clean manual/check order, verified the export pointer referenced the new manual-pending order, marked payment received through Team Mode, and confirmed the queue sent one pending client email, one pending team alert, one receipt client email, and one receipt team alert with one attempt and no visible failure reason. Team Hold, Project Unlock, and AP checkout-started suppression smokes remained queue-silent; bounded queue status checks found no `failed`, `queued`, or `processing` rows.
- Version `903` is a fixture-smoke deployment marker for the already-shipped communication governance tranche. Pre-deploy validation passed, `clasp push --force` reported the Apps Script runtime was already up to date, `clasp version` created `903`, and `clasp deploy` updated the existing stable deployment ID. Direct Apps Script `/exec` and public `/portal` returned HTTP 200, targeted rendered-response checks found no `client_secret`, `clientSecret`, or `hosted_verification_url` markers, the public wrapper still targets the stable deployment ID, and a read-only `PORTAL_EMAIL_QUEUE` sample showed 47 populated rows, all `sent`, attempt count `1`, with no visible failure reason. Comprehensive lifecycle smoke is pending owner-staged fixtures.
- Version `902` is a smoke-ready redeploy of the already-shipped communication governance tranche to the existing stable Apps Script deployment. Pre-deploy validation passed, `clasp push --force` reported the Apps Script runtime was already up to date, `clasp version` created `902`, and `clasp deploy` updated the existing stable deployment ID. Direct Apps Script `/exec` and public `/portal` returned HTTP 200, targeted rendered-response checks found no `client_secret`, `clientSecret`, or `hosted_verification_url` markers, the public wrapper still targets the stable deployment ID, and a read-only `PORTAL_EMAIL_QUEUE` sample showed all populated sampled rows as `sent` with one attempt and no visible failure reason. Comprehensive lifecycle smoke is pending owner-staged fixtures.
- Version `901` deploys the communication governance tranche to the existing stable Apps Script deployment: owner-disabled informational lifecycle emails are policy-suppressed, payment milestone collision guards skip stale competing jobs, email CTAs use public portal/team/AP/status routes, attachment policy distinguishes required from optional files, active email copy uses the standardized no-reply-safe lifecycle shell/copy model, and `tools/report-portal-email-queue-hygiene.mjs` provides a local redacted queue report. Pre-deploy validation passed, `clasp push --force` pushed the four tracked runtime files, `clasp version` created `901`, `clasp deploy` updated the existing stable deployment ID, direct Apps Script `/exec` and public `/portal` returned HTTP 200, and targeted rendered-response checks found no `client_secret`, `clientSecret`, or `hosted_verification_url` markers. Fixture-backed lifecycle smoke is pending owner-staged fixtures.
- Version `900` removes the Gmail alias transport from `sendNotificationEmail_` and sends portal email through `MailApp` with `noReply: true`; post-deploy smoke sent three `sendSummaryEstimatePdfEmail` messages to the approved test client address and each server response reported `transport=mailapp_noreply`, `noReply=true`, no CC recipients, and `ok=true`. Direct Apps Script and public wrapper HTTP smokes returned 200 and did not show `client_secret` / `clientSecret` markers in the targeted checks. Owner reply-bounce verification is pending from the delivered messages.
- Version `899` preserves explicit Team Mode routing through both the public Squarespace wrapper and Apps Script's sandboxed `userCodeAppPanel`: server-rendered request-route metadata now includes `mode` and `teamReview`, the client Team Mode route helper falls back to that boot metadata when the sandbox URL omits query params, and the project route replacement keeps `mode=team` / `teamReview` for explicit Team routes while checkout/setup returns still force client mode. Post-899 browser smoke confirmed the public wrapper keeps `mode=team`, the inner Apps Script frame keeps `mode=team`, the Team Mode password gate appears, the configured Script Property password unlocks the gate, Team admin controls become visible, Save/Place Order remain hidden in Team Mode, ordinary project links stay client-mode, and explicit `teamReview=tax_exempt` routes preserve the review query. The dev badge is `61`.
- Post-899 communication follow-up smoke closed the Team Mode-dependent email gaps: PO payment received, manual/check payment received, Team Hold, Project Unlock, Checkout Reset, PO Reopen, and Project Complete each queued and sent the expected client email plus `hello@redthreads.com` team alert with one attempt and no queue failure reason. Receipt/payment milestones carried invoice/receipt attachment references where required. AP ACH failed/action-needed remains intentionally deferred per owner direction because the hosted Stripe failure path requires a human-completed hCaptcha challenge or another owner-approved Stripe test-event route.
- Version `898` was an initial Team Mode route preservation deployment superseded by `899` after browser smoke showed Apps Script's sandboxed inner frame still needed server-rendered `mode` / `teamReview` metadata.
- Version `897` is a targeted communication-tranche hardening patch on top of `896`: AP ACH payment-link email exceptions after order creation now return the existing structured AP-email-failed response instead of surfacing a generic Apps Script server error. Post-897 smoke confirmed manual/check pending, PO submitted, card paid, card failed/action-needed, standard ACH submitted/pending, standard ACH confirmed/receipt, standard ACH failed/action-needed, AP ACH payment-link send, AP checkout-started, AP ACH submitted/pending, AP ACH confirmed/receipt, and team-to-client chat digest lifecycle paths queue/send expected rows with attachments where required and no rendered `client_secret` / `clientSecret` exposure.
- Version `896` is the full communication-tranche shipment marker. `clasp push --force` reported the Apps Script runtime was already up to date, then version `896` was created and deployed to the existing stable deployment ID. Post-ship smoke confirmed the auth shell, tokenized fixture route, dashboard route, checkout-cancel route, and public wrapper return HTTP 200 with revision `60` markers and no rendered `client_secret` / `clientSecret` exposure. The backing email queue status column showed only `sent` and intentional `skipped` rows in the scanned smoke window.
- Version `895` adds a bounded portal-traffic queue nudge so direct portal loads and chat saves process a small number of due `PORTAL_EMAIL_QUEUE` jobs even if Apps Script time triggers lag. It also preserves trigger fallback for future/due jobs and keeps safe logs free of tokens, raw emails, message text, Stripe secrets, and bank details. The dev badge is `60`.
- Version `894` repairs the email queue scheduler by replacing stale one-off `processPortalEmailQueue` triggers before scheduling a fresh trigger, reducing the chance that queued lifecycle jobs remain unsent after Apps Script trigger drift.
- Version `893` completes the portal communication tranche runtime: standard ACH client emails plus `hello@redthreads.com` team alerts, shared lifecycle shell/progress snapshot, non-ACH card/manual/check/cash/PO lifecycle jobs, AP/order-only ACH lifecycle jobs, 10-minute chat digest batching, and queued non-payment portal lifecycle/admin/account-document events. Queue rows can record `sent`, `failed`, and state-based `skipped` statuses, and lifecycle idempotency includes recipient class plus order/payment/session context where relevant.
- Version `892` adds idempotent standard order-checkout ACH client lifecycle notifications through `PORTAL_EMAIL_QUEUE`: pending/submitted invoice email, confirmed ACH receipt email, and failed/disputed action-needed email. The jobs reuse `sendNotificationEmail_`, existing final invoice attachment helpers, and server invoice artifact generation when no stored invoice PDF exists; AP/order-only ACH is excluded and duplicate success events collapse to one receipt job. The dev badge is `59`.
- Version `891` changes ACH payment-mode Checkout email policy: normal owner and AP/order-only ACH payment sessions no longer attach a Stripe Customer or pass `customer_email` by default, so Stripe asks the payer to enter contact email in hosted Checkout every time. Dashboard ACH setup still uses the portal account Stripe Customer for bank-readiness setup, while ACH payment sessions suppress saved-payment redisplay and `setup_future_usage` unless an explicit internal Customer-attached path opts out of payer-email collection. The AP recipient page no longer asks for an email before Stripe; it uses Stripe as the contact-email collection surface. The dev badge is `58`.
- Version `890` hardens ACH/AP in-flight invoice and payment controls: locked AP/ACH invoice pages now render from durable `PORTAL_ORDERS.orderDraftJson` summary data instead of falling back to empty editable state, AP waiting/checkout-started/ACH verification/ACH processing/paid/disputed/blocked states suppress duplicate normal payment buttons, AP public payment links land on a deliberate invoice/payment page instead of auto-launching Stripe on boot, ACH return routes preserve `summary`/`payNow` request metadata, AP email can CC the purchaser when safe, and AP email preparation uses a blocking transition plus result copy action. Hosted Checkout responses continue to omit `client_secret`. The dev badge is `57`.
- Version `889` restores the approved Credit Terms client/dashboard `Submission Details` rail while keeping the document stage tighter: approved view uses a two-column document/details layout, narrows the document preview column to reduce gutter space, keeps the rail from acting as an outside-click close target, and preserves X/outside-document/Escape close behavior. The dev badge is `56`.
- Version `888` centers the Manage Accounts for ACH Payments modal footer actions so `Done` and `Link Account to Make ACH Payments` sit horizontally centered in the container, with wrapping available for narrower modal widths. The dev badge is `55`.
- Version `887` refines the Dashboard ACH management modal: the header now reads `Manage Accounts for ACH Payments`, the setup CTA reads `Link Account to Make ACH Payments`, empty Ready/Needs action/Unavailable section containers are hidden, and each visible dashboard-saved ACH account has a `Remove` action that soft-hides it by setting `visibilityScope=hidden_from_dashboard` while preserving the underlying saved-bank record. The dev badge is `54`.
- Version `886` fixes the approved Credit Terms document viewer: client/dashboard view now uses a single wide centered document column without the Submission Details rail, and X, outside-document click, and Escape close through a Credit Terms-specific idempotent binder before the viewer opens. Team Review keeps its details/action rail and team-return close path. The dev badge is `53`.
- Version `885` hardens AP ACH payment-link behavior: AP link send/copy state now displays as waiting for Accounts Payable instead of Stripe processing until AP actually starts/completes hosted ACH Checkout; AP checkout-created state has its own waiting-for-AP-Checkout copy; AP email copy uses `Open secure ACH payment page`; and AP email now requires the browser-rendered Summary/Invoice PDF payload instead of silently attaching an older server fallback artifact. The dev badge is `52`.
- Version `884` fixes Dashboard tax exemption document viewer close behavior: opening the approved Michigan sales/use tax document from the account dashboard now binds X, outside-document click, and Escape handling through a tax-document-specific idempotent binder before the viewer opens. The click detector closes from the black space around the document, while clicks inside the document preview remain non-closing. The dev badge is `51`.
- Version `883` fixes Dashboard project-row cursor behavior: project detail text and progress-bar helper/subtext now keep the default cursor and are not selectable, while the project thumbnail remains the expand action, progress stages keep the help cursor, and Open Project keeps the pointer cursor. This deploy also carries deterministic ACH microdeposit hardening: existing Stripe Customers are refreshed with safe account email/name before setup, test fallback uses descriptor-code or amount-mode verification based on Stripe intent metadata, and safely matched `setup_intent.setup_failed` rows move to failed/unavailable. The dev badge is `50`.
- Version `882` suppresses Dashboard ACH setup cancel-return notices: backing out of Stripe hosted setup now returns to the account dashboard without showing `Bank account setup canceled`, without a generic checkout-status notice, and without the bank-setup refresh message. The dev badge is `49`.
- Version `881` completes the Dashboard Manage ACH banks microdeposit finalization path for internal test mode: `getAchMicrodepositVerificationLink` still prefers Stripe-hosted verification URLs, but when Stripe returns no hosted URL and the clicked dashboard-saved bank has validated SetupIntent/PaymentIntent linkage, `STRIPE_MODE=test` can verify through Stripe's official server-side `verify_microdeposits` API and return refreshed safe account/order state. Live mode never auto-verifies microdeposits. The dev badge is `48`.
- Version `880` keeps the Dashboard Manage ACH banks modal open when Add ACH bank is clicked, replaces its contents with a green Stripe setup loading transition using the `GREEN_LOADING.png` asset, suppresses the dashboard-level opening message for that modal path, and restores modal-local errors if setup cannot launch. The dev badge is `47`.
- Version `879` preserves browser saved-password/autofill support on the login/reset auth fields while overriding the browser autofill paint state so filled inputs retain the dark portal field color, text color, caret color, and border treatment. The dev badge is `46`.
- Version `878` fixes Manage ACH banks outside-click behavior across the full app viewport by mounting the backdrop directly under `document.body` before opening and closing on capture-phase pointer events whose target is outside the modal panel. This covers black side gutters outside the dashboard card as well as the dimmed dashboard area. The dev badge is `45`.
- Version `877` fixes Manage ACH banks modal close handling for direct account-dashboard and tokenized dashboard routes by binding X, Done, backdrop click, Add ACH bank, and Escape listeners through an idempotent modal-specific function that runs before the modal opens instead of relying only on the project `wireEvents()` bootstrap. The dev badge is `44`.
- Version `876` fixes the Dashboard `ACH payment accounts` hover cursor so the entire card, including its text, uses the pointer cursor instead of falling back to the text cursor. The dev badge is `43`.
- Version `875` fixes Dashboard Manage ACH banks verification handling: legacy microdeposit-pending rows with only a PaymentMethod ID no longer render a misleading Verify with Stripe button, verification failures display inside the modal instead of the global dashboard message layer, X/Done/backdrop/Escape close the modal after errors, and the server no longer tells users to check a Stripe email when no hosted verification URL exists. The dev badge is `42`.
- Version `874` refines the Dashboard ACH copy: the top-level `ACH payment accounts` card now says `Manage accounts` instead of `Manage connected accounts`, and the ready-state dashboard bullet `ACH bank payments are ready for Stripe checkout...` is no longer rendered. Action-needed and not-set-up ACH guidance bullets remain available. The dev badge is `41`.
- Version `873` completes the Dashboard microdeposit pending verification action: saved ACH method summaries now preserve safe setup/payment intent linkage, pending detection recognizes `microdeposit_pending`, Manage ACH banks sends the clicked row identifiers to `getAchMicrodepositVerificationLink`, the server validates the specific SetupIntent/PaymentIntent against the authorized portal account/order before returning a transient Stripe-hosted verification URL, and older unmatched rows return verification-unavailable guidance instead of guessing. The dev badge is `40`.
- Version `871` refines the dashboard ACH card: the top-level card now reads `ACH payment accounts` with `Manage connected accounts`, removes the readiness paragraph and separate Manage/Add buttons, and opens the existing Manage ACH banks modal when the card is clicked or activated by keyboard. The modal still contains the Add ACH bank action. The dev badge is `39`.
- Version `870` simplifies the ACH user experience: the Dashboard now shows one `ACH Bank Payments` readiness card with Manage ACH banks for safe bank details/actions, while checkout uses generic Stripe-hosted ACH copy with no bank names, last4, default-bank labels, preferred-bank copy, or saved-bank selector. Backend saved-bank redisplay hardening, optional preferred-bank server compatibility, dashboard setup, AP order-only scope, and ACH return/webhook handling remain intact. The dev badge is `38`.
- Version `869` fixes ACH/card checkout success and cancel return routing: all checkout returns force client mode, clear stale Team Mode and one-time payment-route params, avoid dashboard-route resync while reconciling project payment returns, and route successful checkout returns to the invoice Summary view. The dev badge is `37`.
- Version `868` hardens normal saved-bank ACH handoff: validated dashboard-saved ACH method IDs are updated best-effort with Stripe `allow_redisplay=always` plus safe billing name/email before Checkout, newly created normal owner ACH PaymentMethods request `payment_method_data[allow_redisplay]=always`, and portal copy now states that Stripe may ask the user to confirm or choose a bank during hosted Checkout. The dev badge is `36`.
- Version `867` fixes the ACH decision modal bank-list mismatch seen after opening an order from the dashboard: the checkout decision step now merges the project account summary with the hydrated dashboard account summary for the same account, lists every dashboard-visible ACH bank, keeps only usable verified/active dashboard-saved banks selectable, and shows non-ready dashboard banks as disabled rows instead of hiding them. The dev badge is `35`.
- Version `866` moves the ACH pre-checkout decision step into its own sibling full-screen modal container. The ACH decision modal now replaces the checkout-flow modal instead of rendering beneath the payment-selection cards, while keeping the same saved-bank preference, AP payment-link/email behavior, and server Checkout/AP actions. The dev badge is `34`.
- Version `865` adds the ACH pre-checkout decision step: selecting ACH in the order modal opens a two-lane decision view instead of immediately launching Stripe Checkout. Lane 1 validates a dashboard-saved bank preference server-side and stores it only as Checkout/PaymentIntent metadata while hosted Stripe Checkout remains the final bank confirmation surface. Lane 2 prepares a locked, unpaid AP ACH payment-due order and returns or emails only a Red Threads portal payment link; AP checkout remains order-only, suppresses future-save/redisplay, and never creates a dashboard-visible saved bank. The dev badge is `33`.
- Version `864` hardens direct account-dashboard bearer links: account dashboard access by `accountId` or `accountAccessToken` now requires the `PORTAL_ACCOUNTS` row to have account identity metadata or a stable project binding through the existing dashboard visibility rules; unlinked/orphan dashboard links fail with clear account-dashboard error copy instead of rendering a valid-looking blank dashboard shell. The client also keeps dashboard account-load failures visible and stops route canonicalization/setup-return handling when the account load fails. The dev badge is `32`.
- Version `862` fixes Dashboard project-open Team Mode inheritance: server-loaded dashboard/auth project payloads already use client mode, and the client now force-applies them as client mode, clears stale Team Mode unlock/auth state for the opened token, closes the Team Mode gate, and strips stale `mode`/`teamReview` from dashboard-origin route updates. Explicit team URLs and team-admin lifecycle refreshes still preserve Team Mode. The dev badge is `31`.
- Version `861` fixes Dashboard ACH setup cancel-return hydration: dashboard setup success/cancel routes now keep the dashboard hidden behind the loader until `loadProjectsForHome()` has hydrated the account summary and ACH bank state; stored-session setup returns only reveal the dashboard after a successful account load; tokenized/account dashboard return paths then show the setup success/cancel message after the account-bound dashboard is visible. The dev badge is `30`.
- Version `860` fixes Dashboard ACH setup return routing and display after a real setup attempt: hosted setup success/cancel URLs now canonicalize to the public account dashboard route without preserving stale `t=<project-token>` or duplicate dashboard parameters; dashboard boot prefers `accountAccessToken` over `t` when both are present on a dashboard URL; setup-return reconciliation uses the account dashboard credential instead of a stale project token; and the Dashboard ACH area no longer double-rendered a pending microdeposit method as two separate ACH summary cards. The dev badge is `29`.
- Version `859` hardens locked-invoice/AP ACH checkout sequencing: the server now verifies the latest order exists, is locked, and is unpaid before preparing the ACH Stripe Customer or creating a hosted Checkout Session. The dev badge is `28`.
- Version `858` fixes the Dashboard ACH setup return-URL guard for Apps Script runtime contexts where the global `URL` constructor is unavailable or inconsistent. The server still requires a public Red Threads portal URL, but now validates `https://www.redthreads.com/portal` with an Apps Script-safe parser fallback before creating hosted Stripe setup sessions. The dev badge is `27`.
- Version `857` separates dashboard-saved ACH banks from order-only/AP ACH bank evidence. Dashboard setup writes `dashboard_saved` methods only, normal order checkout updates an existing dashboard-saved method but does not create new dashboard-visible banks, AP public payment links use order-only scoping and suppress future-save/redisplay settings, Dashboard Payment Methods lists only dashboard-saved banks, and the client treats `summary=1&payNow=ach` as AP/order-only if an older live wrapper has not yet forwarded `paymentOrigin`.
- Version `855` adds a centralized client route manager so dashboard login and hydration replace the parent URL with an account bearer dashboard route, dashboard-to-project navigation pushes a project route, project-to-dashboard navigation pushes the dashboard route, and one-time return/action params are cleared while durable dashboard/project context is preserved. The repo Squarespace wrapper now uses one route allowlist and can reload the iframe from the current parent URL on browser Back/Forward.
- Version `854` fixes the Dashboard ACH setup false block where server-side public portal URL validation rejected Stripe setup success URLs containing the literal `{CHECKOUT_SESSION_ID}` placeholder; logged-in session dashboards now also promote hydrated account summaries into account bearer return context.
- Version `853` lets a valid tokenized job/dashboard link start or continue Dashboard ACH bank setup without a separate login, promotes hydrated token dashboards to generated account bearer context, validates non-empty public Stripe setup return URLs before calling Stripe, and lets microdeposit verification use the same token/session/account authorization paths.
- Version `852` adds unauthenticated bearer account dashboard links by `accountId` or generated `accountAccessToken`, returns hosted ACH setup success/cancel to the account dashboard through the public portal base URL, and preserves project-origin checkout return behavior.
- Version `851` fixes the dashboard visibility bug where `applyAuthShellRuntimeState()` rebuilt the client auth/dashboard VM without preserving server-rendered feature flags. The Dashboard ACH card now remains gated by `STRIPE_ACH_ENABLED=true` after login and dashboard state resets.
- Version `850` makes the Dashboard ACH entry point explicit by changing the account card from generic Payment Methods copy to an `ACH Bank Account` card, moving it earlier in the account grid, and updating the dev badge to `19`.
- Version `849` adds the Dashboard microdeposit handoff for pending ACH verification: `getAchMicrodepositVerificationLink` retrieves Stripe-hosted microdeposit verification URLs transiently from ACH PaymentIntents or SetupIntents only after ACH evidence is present, the Dashboard Payment Methods card changes pending microdeposit banks into a "Verify with Stripe" action, and the repo Squarespace wrapper allowlist now accepts Stripe-hosted verification URLs as well as Checkout URLs. No hosted verification URL, bank account number, routing number, or microdeposit value is persisted.
- Version `848` completes ACH checkout return reconciliation: `reconcile_checkout_return` / `reconcileCheckoutReturn` verifies Stripe return sessions, locks completed ACH Checkout returns into canonical pending-not-paid state when the webhook has not finalized yet, refreshes ACH setup returns, disables editable/order controls during return reconciliation, and routes locked ACH pending returns to Summary without marking the order paid from the browser return alone.
- Version `847` completes the ACH workflow audit hardening: ACH Checkout Sessions now request saved bank redisplay for Customers, pending/unverified ACH methods no longer become the portal default saved bank, dashboard/summary copy distinguishes bank-verification pending from payment processing, and ACH cancel returns point users back to Stripe-hosted instant verification/manual entry or another payment method.
- Version `846` hardens ACH terminal behavior after review: stale pending/processing Stripe events now preserve paid, failed, disputed, team-hold, in-production, and closed ACH orders; ACH microdeposit `requires_action` is mapped to bank-verification-pending state; and hard ACH failures/disputes mark unsafe saved banks unusable.
- Version `845` hardens ACH V1 after review: Stripe webhook audit URL fields are redacted, locked-invoice ACH checkout checks `STRIPE_ACH_ENABLED` before Customer preparation, and ACH success finalization is deduped across distinct Stripe success event types.
- Version `844`, `ACH V1 infrastructure`, deployed the main ACH V1 surface: `STRIPE_ACH_ENABLED` gating, Stripe Customer persistence on `PORTAL_ACCOUNTS`, hosted ACH payment Checkout with Customer, hosted dashboard bank setup, safe saved-bank summaries, ACH pending-production approval controls, ACH lifecycle copy, and Stripe webhook idempotency through `PORTAL_STRIPE_EVENTS`.
- Sheet infrastructure ensures ACH account headers, ACH order headers, and `PORTAL_STRIPE_EVENTS` through existing infrastructure helpers.
- The repo Squarespace wrapper points to the stable deployment ID and forwards `setupResult` alongside `t`, `checkoutResult`, `stripeSessionId`, `dashboard`, `accountId`, `accountAccessToken`, and `paymentOrigin`.
- ACH is currently enabled for internal Stripe test-mode use through Script Properties. Keep `STRIPE_MODE=test` until a separate live-mode go/no-go.

## Prompt/Historical

- Expected stable Apps Script deployment ID: `AKfycbz9qDgp65f5S3RWhSxGftioMXKKU9O1N0mpHh3waoKY2YyvE72F-cJk-0XYr5YXg4bw`.
- Expected stable public Apps Script URL: `https://script.google.com/macros/s/AKfycbz9qDgp65f5S3RWhSxGftioMXKKU9O1N0mpHh3waoKY2YyvE72F-cJk-0XYr5YXg4bw/exec`.
- Public branded wrapper URL: `https://www.redthreads.com/portal`.
- Live Squarespace wrapper was verified on 2026-06-01 forwarding `t`, `checkoutResult`, `setupResult`, `stripeSessionId`, `dashboard`, `accountId`, `accountAccessToken`, and `paymentOrigin` into the Apps Script iframe.
- Historical calculator sheet ID: `1STzkJjn5WRoBqa5H1KdxAbn4-JCab9DCX4FuZt5HImc`.
- Historical Make scenario: `4062378`.
- Fuller intended architecture includes PORTAL_ORDERS, PORTAL_ACCOUNTS, USERS, USER_SESSIONS, hosted Stripe Checkout, Cloud Run webhook forwarding, Gmail notifications, QuickBooks, and Pipedrive.

## Live Apps Script Alignment

- Browser-verified Apps Script project URL: `https://script.google.com/u/0/home/projects/1zv9lbls_bohme0vDA8EZg4G0dyFrsuv3hHO0NOAijSw9imYYNkqMbkKU/edit`.
- `clasp pull` against the verified live project succeeded on 2026-05-27 and pulled `appsscript.json`, `Code.js`, `Index.html`, and `TaxForm3372Manifest.html`.
- After the binding repair, `clasp push --force`, `clasp version`, and `clasp deploy` can update the live Apps Script source and stable deployment when commands are run sequentially and retried if needed.
- Existing stable deployment ID is present: `AKfycbz9qDgp65f5S3RWhSxGftioMXKKU9O1N0mpHh3waoKY2YyvE72F-cJk-0XYr5YXg4bw`, currently deployed at version 889, `Restore credit terms details rail`.
- Stage 3A checkout performance work was deployed to the stable Apps Script deployment on 2026-05-28 after sequential `clasp push`, `clasp version`, and `clasp deploy` commands completed. The normal Place Order card checkout path still performs validation, Stripe session creation, PORTAL_ORDERS write, EXPORT_LOG pointer write, and competing unpaid order supersede before returning `checkoutUrl`, but supersede now writes each known candidate row once and returns row info from the in-memory updated values.
- Stage 3B0 checkout timing observability was deployed to the stable Apps Script deployment on 2026-05-28 as version 837. This was instrumentation only: checkout behavior, Stripe payloads, order persistence, webhook logic, lifecycle state, schema, and Squarespace wrapper navigation were not changed. Browser console output now includes `[RT-CHECKOUT-TIMING-CLIENT-SUMMARY]`; Apps Script server logs now include `[RT-CHECKOUT-TIMING-SERVER-SUMMARY]`.
- Desktop-only mobile blocking was deployed to the stable Apps Script deployment on 2026-05-28 as version 838. This is UI/access guard only: Stripe payloads, order persistence, webhook logic, lifecycle state, schema, Apps Script config, and `Code.js` were not changed. Direct Apps Script and iframe-rendered Portal views under `900px` now show the Red Threads mobile block with copy-link controls.
- Mobile block link cleanup was deployed to the stable Apps Script deployment on 2026-05-28 as version 839. The visible URL is now centered plain text with single-line ellipsis while the copy button still copies the full current URL. This was UI-only: `Code.js`, Stripe payloads, order persistence, webhook logic, lifecycle state, schema, and Apps Script config were not changed.
- Checkout unload-prompt suppression was deployed to the stable Apps Script deployment on 2026-05-28 as version 840. The native browser unsaved-changes prompt is bypassed only while an approved Stripe checkout navigation is active, and the dirty baseline is reset after a successful checkout session response with a valid checkout URL. This was client-side runtime UI/state only: `Code.js`, Stripe payloads, order persistence, webhook logic, lifecycle state, schema, and Apps Script config were not changed.
- ACH checkout selection was deployed to the stable Apps Script deployment on 2026-05-28 as version 841. The existing ACH Checkout Session path is now exposed in the order checkout flow and locked invoice payment controls by enabling the shared ACH client gate. ACH sessions continue to use Stripe Checkout with `us_bank_account`; async webhook/currentness handling remains unchanged.
- Checkout-return welcome modal suppression was deployed to the stable Apps Script deployment on 2026-05-29 as version 842. When Stripe returns with `checkoutResult=success` or `checkoutResult=cancel`, the app now preserves that return intent for intro suppression before consuming and removing the checkout URL params.
- Checkout-return request metadata was deployed to the stable Apps Script deployment on 2026-05-29 as version 843. `doGet(e)` now passes safe `checkoutResult` and `stripeSessionId` request-route metadata into the rendered Portal VM so the initial app boot can suppress the welcome intro before URL cleanup or parent iframe messaging completes.
- ACH V1 infrastructure was deployed to the stable Apps Script deployment as version 844, hardened and redeployed as versions 845 and 846 on 2026-06-01, completed as version 847 to strengthen saved-bank redisplay/defaulting and verification/cancel UX, completed as version 848 to add server/client ACH return reconciliation for pending-not-paid Checkout returns, completed as version 849 to add Dashboard-to-Stripe hosted microdeposit verification handoff, completed as version 850 to make the dashboard ACH card explicit, completed as version 851 to preserve ACH feature flags through dashboard auth-shell state resets, completed as version 852 to add account-dashboard deep links and ACH setup dashboard returns, completed as version 853 to allow tokenized dashboards to start ACH setup and guard Stripe setup return URLs, completed as version 854 to allow Stripe's setup-session placeholder in the return URL guard, completed as version 855 to add durable dashboard/project parent URL routing, completed as version 857 to separate dashboard-saved and AP/order-only ACH bank scopes, completed as version 858 to make public portal return URL validation Apps Script-safe for Dashboard ACH setup, completed as version 859 to delay locked-invoice/AP ACH Customer preparation until after locked/unpaid order validation, completed as version 860 to canonicalize Dashboard ACH setup returns and remove duplicate pending/saved ACH dashboard cards, completed as version 861 to keep setup cancel/success dashboard returns hidden until account hydration completes, completed as version 862 to prevent dashboard/auth project opens from inheriting stale Team Mode, completed as version 864 to reject unlinked direct account-dashboard bearer links instead of rendering blank dashboard shells, completed as version 865 to add the ACH pre-checkout decision step and AP payment-link/email lane, completed as version 866 to move that ACH decision step into a separate modal container, completed as version 867 to list all dashboard ACH banks in the decision modal while keeping non-ready banks disabled, completed as version 868 to harden Stripe saved-bank redisplay eligibility and make the saved-bank handoff copy explicitly preference-based, completed as version 869 to force checkout returns back to client mode and the invoice Summary route, completed as version 870 to make Dashboard the ACH readiness/action-needed surface and checkout a generic Stripe ACH payment path, completed as version 871 to make the dashboard ACH card itself the Manage ACH banks action, completed as version 873 to preserve safe microdeposit verification linkage, completed as version 874 to refine ACH dashboard copy, completed as version 875 to fix modal-local ACH verification fallback and close handling, completed as version 876 to fix the ACH dashboard card cursor, completed as version 877 to fix Manage ACH banks close binding on dashboard-only routes, completed as version 878 to fix Manage ACH banks outside-click close behavior in the full viewport, completed as version 880 to add the modal-local green ACH setup transition, completed as version 881 to add server-only Stripe test microdeposit verification fallback, completed as version 882 to silence Dashboard ACH setup cancel-return notices, completed as version 883 to fix Dashboard project row cursor behavior, completed as version 884 to fix Dashboard tax document viewer close/outside-click handling, completed as version 885 to harden AP ACH payment-link display, email copy, and rendered Summary/Invoice attachment handling, completed as version 886 to fix the approved Credit Terms document viewer layout and close handling, completed as version 887 to refine the ACH account management modal and add client-visible soft hiding for dashboard-saved ACH accounts, completed as version 888 to center the ACH management modal footer actions, completed as version 889 to restore the approved Credit Terms details rail while keeping the document preview tighter, completed as version 890 to harden ACH/AP in-flight invoice and payment controls, completed as version 891 to let Stripe collect ACH payer contact email in Checkout, and completed as version 892 to add standard ACH client lifecycle notification emails.
- The previous `Requested entity was not found` blocker was traced to the stale local `.clasp.json` binding, not to the stable deployment ID itself.

## Blocked Or Unverified

- The live-pulled server source had a checked-in Team Mode default credential; the repo copy now requires the Team Mode password to come from Apps Script Script Properties instead. Confirm that property before relying on Team Mode in production.
- Public stable URL smoke test confirmed the deployed HTML contains `Development revision 18`, omits stale `Development revision 17`, and carries `requestRoute.checkoutResult=cancel` in the VM for a checkout cancel return URL.
- Stage 2 controlled checkout timing on a disposable/test project confirmed `fastCheckoutResponse: true` and `hydrationSkipped: true`. Response hydration no longer blocks normal Stripe navigation; the observed click-to-navigation time was 18,258 ms, with server total 15,960 ms, `google.script.run` round trip 18,189 ms, Stripe API call 710 ms, PORTAL_ORDERS write 262 ms, EXPORT_LOG pointer write 514 ms, and competing unpaid order supersede 7,412 ms.
- Stage 3A post-ship inspection in the Apps Script executions view showed version 836 `createCheckoutAttempt` executions, including a 12.177 s completed execution after deployment. A disposable/test checkout artifact was created and no payment was completed. Detailed server timing JSON was not retrievable through `clasp logs` because the GCP project ID is not configured, and the Apps Script executions UI did not expose parseable log details through the available inspection path.
- Stage 3B0 controlled smoke on a disposable/test project produced the client flat line: `clickToLoader=19`, `clientCapture=2`, `payloadBuild=0`, `serverRoundTrip=12276`, `responseToUrl=0`, `navigationAssign=7`, `totalClickToNavigation=12352`, `launched=true`, `checkoutReady=true`. The response timing object reported server `totalMs=8236`, `stripeApi=938`, `orderWrite=404`, `exportPointerWrite=603`, `supersede=619`, and `hydrationSkipped=true`. The automation environment did not leave the Apps Script sandbox for Stripe despite `launched=true`, so same-window visual navigation should be rechecked manually through the branded wrapper; no payment was completed and no false paid state was observed.
- `clasp logs --json` is still blocked by missing GCP project ID, so the flat server summary line is deployed but was not retrieved through clasp during Stage 3B0.
- Full lifecycle/payment fixture regression was not run during the Stage 3A checkout performance Full ship.
- Live Squarespace `/portal` inspection on 2026-05-28 showed the fullscreen iframe and `RT_PORTAL_ROUTE_REPLACE` / `RT_PORTAL_NAVIGATE` wrapper handlers present after the Squarespace snippet was corrected.
- The repo wrapper has the cleaned pre-iframe mobile block, but Squarespace must be manually updated from `web/squarespace-portal-code-block.html` for that wrapper-layer block to be live. Until then, the deployed Apps Script app-level block handles narrow wrapper access after the iframe loads.
- ACH V1 non-transactional live-surface smoke passed on 2026-06-01 after version 845 deployment: direct `/exec`, direct token load, public wrapper, and tokenized public wrapper returned 200; the public wrapper target matched the stable deployment ID; decoded VM reported `achPaymentEnabled=true` and an ACH pending fixture remained `currentPaymentState=pending`, not paid.
- ACH V1 post-846 smoke confirmed the direct Apps Script `/exec` and public wrapper `/portal` returned 200, and the live wrapper still targets the stable deployment ID while forwarding `setupResult`, `checkoutResult`, and `stripeSessionId`. No bank payment flow was initiated during the version 846 smoke.
- ACH V1 post-847 smoke confirmed direct Apps Script `/exec` returned 200 and contains the new ACH cancel and bank-verification-pending copy; public wrapper `/portal` returned 200 and still targets the stable deployment ID while forwarding `setupResult`, `checkoutResult`, and `stripeSessionId`. No bank payment flow was initiated during the version 847 smoke.
- ACH V1 post-848 smoke confirmed direct Apps Script `/exec` returned 200, advertises the new `reconcileCheckoutReturn` server function, and public wrapper `/portal` returned 200 while still targeting the stable deployment ID and forwarding `setupResult`, `checkoutResult`, and `stripeSessionId`. No bank payment flow, tokenized fixture payment, or real payment was initiated during this ship.
- ACH V1 post-849 smoke confirmed direct Apps Script `/exec` returned 200 and advertises the new `getAchMicrodepositVerificationLink` server function; public wrapper `/portal` returned 200 and still targets the stable deployment ID. No bank flow, tokenized fixture payment, or real payment was initiated during this ship.
- ACH V1 post-850 smoke confirmed direct Apps Script `/exec` returned 200 and contains `Development revision 19`, `ACH Bank Account`, `Connect ACH bank`, `Add ACH bank`, and `achPaymentEnabled`; public wrapper `/portal` returned 200, still targets the stable deployment ID, and includes the Stripe-hosted payment/verification navigation allowlist. No bank flow, tokenized fixture payment, or real payment was initiated during this ship.
- ACH V1 post-851 smoke confirmed direct Apps Script `/exec` returned 200 and contains `Development revision 20`, `ACH Bank Account`, and `achPaymentEnabled`; the in-app browser public wrapper reload showed dev badge `20`. The reload landed on the login screen, so authenticated dashboard visual confirmation remains for the owner after login.
- ACH V1 post-852 smoke confirmed direct Apps Script `/exec` returned 200 and contains `Development revision 21`, `accountAccessToken`, `createAchSetupSession`, and `reconcileCheckoutReturn`; direct Apps Script account-dashboard URL rendering returned 200 with account-route metadata. Public wrapper `/portal` returned 200 and still targets the stable deployment ID, but the live Squarespace wrapper did not yet forward `dashboard` or `accountId` into the iframe; refresh live Squarespace from the repo wrapper before testing public account-dashboard links.
- ACH V1 post-853 smoke confirmed direct Apps Script `/exec` returned 200 and exposes `createAchSetupSession` and `getAchMicrodepositVerificationLink`; public wrapper `/portal` returned 200 and targets the stable deployment ID; in-app browser smoke confirmed `https://www.redthreads.com/portal?dashboard=1&accountId=acct_invalid_smoke` forwards `dashboard=1` and `accountId` into the stable Apps Script iframe. No bank flow, tokenized fixture payment, or real payment was initiated during this ship.
- ACH V1 post-854 smoke confirmed direct Apps Script `/exec` returned 200 and public wrapper `/portal` returned 200 while targeting the stable deployment ID. No bank flow, tokenized fixture payment, or real payment was initiated during this ship.
- Post-855 smoke confirmed direct Apps Script `/exec` and public wrapper `/portal` returned HTTP 200; direct Apps Script contains `Development revision 24` and the new route helper. In-app browser inspection of the live public wrapper still showed the older wrapper without `ROUTE_PARAM_KEYS` / `popstate`, so live parent Back/Forward route reload depends on refreshing the live Squarespace code block from the repo wrapper because wrapper changes are not deployed by `clasp`.
- Post-857 smoke confirmed direct Apps Script `/exec` and public wrapper `/portal` returned HTTP 200; direct Apps Script contains `Development revision 26`, `Copy ACH Payment Link`, `setDefaultAchPaymentMethod`, `PAYMENT_ORIGIN_PARAM_RAW`, `paymentOrigin`, and `visibilityScope`; stale `Development revision 25` is absent. The live public wrapper still did not show the new `paymentOrigin` allowlist at smoke time, so version 857 includes a defensive client fallback that treats `summary=1&payNow=ach` as AP/order-only until the live Squarespace wrapper is refreshed from the repo copy.
- Post-858 smoke confirmed direct Apps Script `/exec` and public wrapper `/portal` returned HTTP 200; direct Apps Script contains `Development revision 27` and omits stale `Development revision 26`; public wrapper targets the stable deployment ID and includes `paymentOrigin`. No bank setup flow, tokenized fixture payment, or real payment was initiated during this ship.
- Post-859 smoke confirmed direct Apps Script `/exec` and public wrapper `/portal` returned HTTP 200; direct Apps Script contains `Development revision 28`, omits stale `Development revision 27`, and still exposes `Copy ACH Payment Link`, `paymentOrigin`, and `visibilityScope`; public wrapper targets the stable deployment ID and includes `paymentOrigin` plus `RT_PORTAL_NAVIGATE`. No bank setup flow, tokenized fixture payment, real payment, Script Properties change, live-mode switch, or Squarespace update was performed during this ship.
- Post-860 smoke confirmed direct Apps Script `/exec` and public wrapper `/portal` returned HTTP 200; direct Apps Script contains `Development revision 29`, omits stale `Development revision 28`, and exposes `Add ACH Bank` plus `ACH Bank Verification`; public wrapper targets the stable deployment ID and includes `accountAccessToken` plus `paymentOrigin`. In-app browser automation was unavailable in this Codex session, so the owner should manually retry Dashboard Add ACH Bank through the live wrapper. No bank setup flow, tokenized fixture payment, real payment, Script Properties change, live-mode switch, or Squarespace update was performed during this ship.
- Post-861 smoke confirmed direct Apps Script `/exec` and public wrapper `/portal` returned HTTP 200; direct Apps Script contains `Development revision 30`, omits stale `Development revision 29`, and includes the setup cancel dashboard copy; public wrapper targets the stable deployment ID and includes `accountAccessToken`, `setupResult`, `paymentOrigin`, and `RT_PORTAL_NAVIGATE`. In-app browser automation was unavailable because the Browser backend was closed, so the owner should manually retry Dashboard Add ACH Bank cancel-return through the live wrapper. No bank setup completion, tokenized fixture payment, real payment, Script Properties change, live-mode switch, or Squarespace update was performed during this ship.
- Post-862 smoke confirmed direct Apps Script `/exec` and public wrapper `/portal` returned HTTP 200; direct Apps Script contains `Development revision 31`, omits stale `Development revision 30`, and contains `forceClientMode` plus `clearTeamModeRuntimeStateForToken_`; public wrapper targets the stable deployment ID and still forwards explicit `mode`/`teamReview` for real team links. After owner clarification that current portal orders/accounts are test fixtures available for build testing, Codex automated the local order/project 1900 direct project fixture through the public wrapper: it loaded in client mode with no Team Mode password gate, the status copy said to click Place Order, and the Place Order button was visible/enabled. A separately tried account bearer route resolved to a blank account shell before version 863, so it was not treated as the logged-in account-dashboard fixture at that time.
- Post-864 smoke confirmed direct Apps Script `/exec` and public wrapper `/portal` returned HTTP 200; direct Apps Script contains `Development revision 32` and `dashboard-load-failed`; the stable deployment ID points to version `864`. Public-wrapper browser smoke confirmed the previously blank account bearer route now resolves to an account-bound dashboard with organization/email/project rows, a deliberately invalid account-dashboard route shows a persistent dashboard-link error instead of silently acting as a valid dashboard, and the order/project 1900 fixture still loads as client mode with no Team Mode password gate and Place Order visible/enabled. No bank setup flow, tokenized fixture payment, real payment, Script Properties change, live-mode switch, Squarespace update, snapshot JSON mutation, or EXPORT_LOG schema change was performed during this ship.
- Post-865 smoke confirmed direct Apps Script `/exec` returned HTTP 200, contains `Development revision 33`, and exposes `orderFlowAchDecisionSection`, `preferredAchPaymentMethodId`, `prepareAchPaymentLinkToAp`, and `sendAchPaymentLinkToAp`; public wrapper `/portal` returned HTTP 200, targets the stable deployment ID, and includes `accountAccessToken`, `setupResult`, `paymentOrigin`, and `RT_PORTAL_NAVIGATE`. No bank setup flow, tokenized fixture payment, real payment, Script Properties change, live-mode switch, Squarespace update, snapshot JSON mutation, or EXPORT_LOG schema change was performed during this ship.
- Post-866 smoke confirmed direct Apps Script `/exec` returned HTTP 200, contains `Development revision 34`, and exposes `orderFlowAchDecisionModal`, `orderFlowAchDecisionSection`, and `preferredAchPaymentMethodId`; public wrapper `/portal` returned HTTP 200, targets the stable deployment ID, and includes `accountAccessToken`, `paymentOrigin`, and `RT_PORTAL_NAVIGATE`. No bank setup flow, tokenized fixture payment, real payment, Script Properties change, live-mode switch, Squarespace update, snapshot JSON mutation, or EXPORT_LOG schema change was performed during this ship.
- Post-867 smoke confirmed direct Apps Script `/exec` returned HTTP 200, contains `Development revision 35`, omits stale `Development revision 34`, and exposes `getOrderFlowAchAccountSummaryForUi_` plus the disabled-bank decision copy; public wrapper `/portal` returned HTTP 200, targets the stable deployment ID, and includes `paymentOrigin`. No bank setup flow, tokenized fixture payment, real payment, Script Properties change, live-mode switch, Squarespace update, snapshot JSON mutation, or EXPORT_LOG schema change was performed during this ship.
- Post-868 smoke confirmed direct Apps Script `/exec` returned HTTP 200, contains `Development revision 36`, omits stale `Development revision 35`, and includes the new saved-bank preference copy that says Stripe may ask the user to confirm or choose a bank before payment is initiated; public wrapper `/portal` returned HTTP 200, targets the stable deployment ID, and includes `paymentOrigin` plus `RT_PORTAL_NAVIGATE`. No bank setup flow, tokenized fixture payment, real payment, Script Properties change, live-mode switch, Squarespace update, snapshot JSON mutation, or EXPORT_LOG schema change was performed during this ship.
- Post-869 smoke confirmed direct Apps Script `/exec` returned HTTP 200, contains `Development revision 37`, omits stale `Development revision 36`, and includes the checkout-return client-mode guard. Public wrapper `/portal` returned HTTP 200, targets the stable deployment ID, and includes `checkoutResult` plus `RT_PORTAL_NAVIGATE`. No bank setup flow, tokenized fixture payment, real payment, Script Properties change, live-mode switch, Squarespace update, snapshot JSON mutation, or EXPORT_LOG schema change was performed during this ship.
- Post-870 smoke confirmed direct Apps Script `/exec` returned HTTP 200, contains `Development revision 38`, omits stale `Development revision 37`, includes `ACH Bank Payments`, `Manage ACH banks`, and the generic Stripe-hosted ACH copy, and omits the old wallet-like ACH labels. Public wrapper `/portal` returned HTTP 200, targets the stable deployment ID, and includes `paymentOrigin` plus `RT_PORTAL_NAVIGATE`. No bank setup flow, tokenized fixture payment, real payment, Script Properties change, live-mode switch, Squarespace update, snapshot JSON mutation, or EXPORT_LOG schema change was performed during this ship.
- Post-871 smoke confirmed direct Apps Script `/exec` returned HTTP 200, contains `Development revision 39`, omits stale `Development revision 38`, includes `ACH payment accounts`, `Manage connected accounts`, `Manage ACH banks`, and modal `Add ACH bank`, and omits the removed top-card readiness sentence and top-card ACH action button class. Public wrapper `/portal` returned HTTP 200, targets the stable deployment ID, and includes `paymentOrigin` plus `RT_PORTAL_NAVIGATE`. The in-app browser was on a blank tab, so authenticated dashboard click-through was not automated in-browser during this ship. No bank setup flow, tokenized fixture payment, real payment, Script Properties change, live-mode switch, Squarespace update, snapshot JSON mutation, or EXPORT_LOG schema change was performed.
- Post-879 smoke confirmed direct Apps Script `/exec` returned HTTP 200, contains `Development revision 46`, omits stale `Development revision 45`, and includes the scoped `:-webkit-autofill` styling. Source smoke confirmed the login email and password fields still preserve `autocomplete` attributes, so browser saved credentials remain available. Public wrapper `/portal` returned HTTP 200, targets the stable deployment ID, and includes `RT_PORTAL_NAVIGATE`. No login credential capture, bank setup flow, tokenized fixture payment, real payment, Script Properties change, live-mode switch, Squarespace update, snapshot JSON mutation, or EXPORT_LOG schema change was performed.
- Post-880 smoke confirmed direct Apps Script `/exec` returned HTTP 200, contains `Development revision 47`, omits stale `Development revision 46`, and includes the green Manage ACH setup loading transition. Source smoke confirmed the modal Add ACH bank path no longer closes the modal before setup and suppresses the dashboard-level opening message for the modal source. Public wrapper `/portal` returned HTTP 200, targets the stable deployment ID, and includes `RT_PORTAL_NAVIGATE`. No bank setup completion, tokenized fixture payment, real payment, Script Properties change, live-mode switch, Squarespace update, snapshot JSON mutation, or EXPORT_LOG schema change was performed.
- Post-881 smoke confirmed direct Apps Script `/exec` returned HTTP 200, contains `Development revision 48`, omits stale `Development revision 47`, and includes the modal success copy for server-side test verification. Public wrapper `/portal` returned HTTP 200, targets the stable deployment ID, and includes `RT_PORTAL_NAVIGATE` plus `paymentOrigin`. The stable deployment list shows `AKfycbz9qDgp65f5S3RWhSxGftioMXKKU9O1N0mpHh3waoKY2YyvE72F-cJk-0XYr5YXg4bw @881 - Add ACH test microdeposit verification fallback`. Direct function-level fallback smoke could not be automated because the web-app POST redirect returned Google HTML to curl and the Apps Script project is not API-executable for `clasp run`; the in-browser `google.script.run` path remains the manual smoke path. No Script Properties change, live-mode switch, Squarespace update, snapshot JSON mutation, EXPORT_LOG schema change, or new deployment ID was performed.
- Post-882 smoke confirmed direct Apps Script `/exec` returned HTTP 200, contains `Development revision 49`, omits stale `Development revision 48`, and no longer contains `Bank account setup canceled`. Public wrapper `/portal` returned HTTP 200, targets the stable deployment ID, and includes `RT_PORTAL_NAVIGATE` plus `setupResult` forwarding. No bank setup flow, payment flow, Script Properties change, live-mode switch, Squarespace update, snapshot JSON mutation, EXPORT_LOG schema change, or new deployment ID was performed.
- Targeted ACH sensitive-data search found only redaction/safety/documentation references plus pre-existing tax form direct-pay fields outside the ACH storage path; no new ACH storage path writes full bank account numbers, routing numbers, microdeposit values, or raw Financial Connections data.

## Workflow

- Current preferred workflow is mainline-first for owner-directed docs/tooling, runtime edits, and Full ship requests.
- Branch/PR workflow is optional and should be used only when explicitly requested, required by repo protection, or appropriate for high-risk architecture/review work.
- Full ship workflow is Apps Script-first: validate locally, run `clasp status`, `clasp push --force`, `clasp version`, `clasp deploy`, deployment verification, and smoke test before GitHub push.
- GitHub source updates and Apps Script push/version/deploy currently work from the repaired local clasp binding when the Apps Script sequence is run in order and retried if a transient command failure occurs.

## Known Follow-Ups

- Continue internal Stripe test-mode ACH flight testing before any live-mode pilot: dashboard setup, ACH success/failure, microdeposit fallback if presented, approved-account pending-production exception, and webhook replay/idempotency.
- Continue public-wrapper route smoke for dashboard Back/Forward and AP ACH payment links before any live-mode pilot.
- `schemas/snapshot_v2_0_0.schema.json` allows `printJobs.minItems: 0`, while current project docs/prompt say the supported V2 family is 1-4 PrintJobs. Do not change the schema without a separate schema/contract correction task.
